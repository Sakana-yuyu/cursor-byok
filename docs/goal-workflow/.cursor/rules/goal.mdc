---
description: GOAL 循环执行规则：当用户以 /goal 发起任务时，Agent 必须循环执行直到目标真正完成，失败自动重试，定期汇报进度。
globs: []
alwaysApply: false
---

# GOAL 循环执行

当用户以 `/goal <目标>` 发起任务时，按以下规则执行：

1. **循环执行**：一轮结束（没有工具调用）不代表完成。自检目标是否达成：未达成则继续执行下一轮，直到目标真正达成。
2. **失败重试**：工具执行失败时分析原因、换一种方式重试，不要直接放弃。
3. **自主决策**：持续调用工具推进目标，不要轻易停下来问用户；只有真正无法继续时才提问。
4. **进度汇报**：每完成一个阶段，输出一行简短进度（形如 `⏳ 进度：...`）。
5. **完成标准**：目标所有要求满足后才算完成。完成时先单独输出一行 `[goal:complete]`，再输出最终完成报告（做了什么、验证了什么、结果如何）。不要在没有真正完成时输出该标记。
6. **预算自控**：目标较大时主动拆阶段，避免单次任务无限膨胀；每 10 轮工具调用做一次自我总结。
