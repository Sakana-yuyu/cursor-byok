# Goal 循环执行（codex-style goal）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 cursor-byok 支持 goal 模式——agent 带明确目标循环执行（失败自动重试、自主决策、预算保护）直到任务真正完成；进度与结果在官方 Cursor 对话流（SSE 注入）和前端 Goal 面板双端可见；同时交付一份官方 Cursor 的 Rules/Command 组合模板作为不改代码的近似方案。

**Architecture:** 复用现有 provider pass 循环（`handleRunIntent → driveProvider → handleProviderDoneEvent → providerActionResume`）。在 `ActiveStream` 上挂可选 `Goal *GoalState`（nil = 非 goal 会话，全路径旁路，非 goal 行为零改动）。goal 会话在"无工具调用的 pass 收尾点"接管：预算检查 → 自检提示续跑 → 校验子代理二次确认（复用 `delegation.Scheduler` 同步任务）→ 通过才收口。进度经 summary 事件注入对话流；goal 状态存 `Service` 内存 map，前端经新 wails bindings 读写。

**Tech Stack:** Go 1.25+（`cursor` module）、Vue3 + Vite + wails3 bindings（frontend/）、connectrpc SSE（`broker.Publish` → RunSSE）。

## Global Constraints

- **旁路约束**：所有 goal 逻辑必须以 `stream.Goal != nil` 为前提；非 goal 会话走原逻辑，行为完全不变。
- **配置注入**：forwarder 包不直接 import `internal/backend/server/config`；goal 运行时配置经接口 `goalConfigProvider` 注入（仿现有 `delegation.RuntimeConfigProvider`，见 `service.go:162-165` 的断言模式）。`GoalRuntimeConfig` 类型定义在 forwarder 包。
- **goal 状态仅内存**（MVP）：`Service.goals` map 保留最近 100 条，不写 conversation 文件；重连/重启后旧 goal 记录丢失，文档注明。
- **预算语义**：`MaxProviderPasses` 与 `MaxDurationSeconds` 始终生效；`MaxCostUSD` 仅在 host 层提供 pricing provider（`historymetrics.PriceLookup`）时生效，否则跳过费用检查。
- **完成判定**：连续 `SelfCheckPasses` 次无工具调用 pass → 校验子代理（Readonly，输出首行 `VERIFIED` / `NOT_VERIFIED`）→ 通过才 `GoalStatusCompleted`。
- **前端文本**：所有用户可见文案走 `npm --prefix frontend run i18n:scan` 自动生成 key，不手写 i18n key。
- **wails bindings**：新增 Go binding 方法后需重跑 wails3 构建生成 `frontend/bindings/`；browser-preview 模式需同步补 `frontend/src/services/browserBindings.js` mock。
- **常量**：goal 前缀 `"/goal"`、`"#goal"`；默认 `MaxProviderPasses=30`、`SelfCheckPasses=2`、`VerifyMaxRetries=3`、`ErrorMaxRetries=3`、`ProgressInterval=5`、`MaxDurationSeconds=0`（不限）、`MaxCostUSD=0`（不限）、`Enabled=false`。
- **测试风格**：Go 测试与被测代码同包（`package forwarder`）白盒表驱动；前端验证用 `npm --prefix frontend run build`。

---

### Task 1: Goal 状态类型与运行时配置注入

**Files:**
- Create: `internal/backend/forwarder/goal.go`（类型与常量）
- Create: `internal/backend/forwarder/goal_test.go`
- Modify: `internal/backend/forwarder/service.go:51-78`（Service struct 加 `goalConfig goalConfigProvider`、`goalsMu`、`goals`）、`:128-210`（NewService 断言注入）
- Modify: `internal/backend/server/config/types.go`（GoalConfig 字段 + 默认 + normalize）
- Modify: `internal/backend/host.go`（host.configs 实现 `GoalRuntimeConfig()`）

**Interfaces:**
- Produces: `type GoalStatus string`（常量 `GoalStatusRunning/Completed/Failed/BudgetExceeded/Stopped`）；`type GoalState struct`；`func newGoalState(conversationID, goalText string, strict bool) *GoalState`；`type GoalRuntimeConfig struct`；`func defaultGoalRuntimeConfig() GoalRuntimeConfig`；`type goalConfigProvider interface { GoalRuntimeConfig() GoalRuntimeConfig }`。后续任务全部依赖这些符号。

- [ ] **Step 1: Write the failing test**

`internal/backend/forwarder/goal_test.go`:

```go
package forwarder

import (
	"testing"
	"time"
)

func TestNewGoalState(t *testing.T) {
	state := newGoalState("conv-1", "  修复所有测试  ")
	if state.ConversationID != "conv-1" {
		t.Fatalf("conversation id = %q, want conv-1", state.ConversationID)
	}
	if state.GoalText != "修复所有测试" {
		t.Fatalf("goal text = %q, want trimmed", state.GoalText)
	}
	if state.Status != GoalStatusRunning {
		t.Fatalf("status = %q, want running", state.Status)
	}
	if state.StartedAt.IsZero() || state.UpdatedAt.IsZero() {
		t.Fatal("timestamps must be set")
	}
}

func TestDefaultGoalRuntimeConfig(t *testing.T) {
	cfg := defaultGoalRuntimeConfig()
	if cfg.MaxProviderPasses != 30 {
		t.Fatalf("max provider passes = %d, want 30", cfg.MaxProviderPasses)
	}
	if cfg.SelfCheckPasses != 2 {
		t.Fatalf("self check passes = %d, want 2", cfg.SelfCheckPasses)
	}
	if cfg.VerifyMaxRetries != 3 || cfg.ErrorMaxRetries != 3 {
		t.Fatalf("retry defaults wrong: verify=%d error=%d", cfg.VerifyMaxRetries, cfg.ErrorMaxRetries)
	}
	if cfg.ProgressInterval != 5 {
		t.Fatalf("progress interval = %d, want 5", cfg.ProgressInterval)
	}
	if cfg.MaxDuration != 0 || cfg.MaxCostUSD != 0 || cfg.Enabled {
		t.Fatalf("unlimited/defaults wrong: %+v", cfg)
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/backend/forwarder/ -run 'TestNewGoalState|TestDefaultGoalRuntimeConfig' -v`
Expected: FAIL with "undefined: newGoalState / defaultGoalRuntimeConfig"

- [ ] **Step 3: Write minimal implementation**

Create `internal/backend/forwarder/goal.go`:

```go
package forwarder

import (
	"strings"
	"time"
)

// GoalStatus 是 goal 会话的终态/运行态枚举。只在内存维护（MVP），不持久化。
type GoalStatus string

const (
	GoalStatusRunning        GoalStatus = "running"
	GoalStatusCompleted      GoalStatus = "completed"
	GoalStatusFailed         GoalStatus = "failed"
	GoalStatusBudgetExceeded GoalStatus = "budget_exceeded"
	GoalStatusStopped        GoalStatus = "stopped"
)

// GoalState 挂在 ActiveStream.Goal 上（nil = 非 goal 会话）。
type GoalState struct {
	ConversationID string
	GoalText       string
	Status         GoalStatus
	Strict         bool // /goal --strict：校验不通过时不允许模型自检兜底（借鉴 Reasonix Strict）
	ProviderPasses int
	ToolCalls      int
	SelfChecks     int
	RetryCount     int // 校验子代理未通过的重试次数
	ErrorRetries   int // provider 错误重试次数
	StaleCount     int // 校验连续未通过次数，达到阈值后提示结构性换策略（借鉴 Reasonix stale pivot）
	CostEstimateUSD float64
	StartedAt      time.Time
	UpdatedAt      time.Time
	LastProgress   string
	CompletionText string
	StopReason     string

	consecutiveIdle int  // 连续无工具调用 pass 计数
	CompletionClaimed bool // 模型已输出 [goal:complete] 声明
}

func newGoalState(conversationID, goalText string, strict bool) *GoalState {
	now := time.Now().UTC()
	return &GoalState{
		ConversationID: conversationID,
		GoalText:       strings.TrimSpace(goalText),
		Status:         GoalStatusRunning,
		Strict:         strict,
		StartedAt:      now,
		UpdatedAt:      now,
	}
}

// GoalRuntimeConfig 是 forwarder 运行时消费的 goal 配置，由 host 层从
// server/config 的持久化结构转换而来（仿 delegation.RuntimeConfig）。
type GoalRuntimeConfig struct {
	Enabled           bool
	MaxProviderPasses int
	MaxDuration       time.Duration
	MaxCostUSD        float64
	SelfCheckPasses   int
	VerifyMaxRetries  int
	ErrorMaxRetries   int
	ProgressInterval  int
}

func defaultGoalRuntimeConfig() GoalRuntimeConfig {
	return GoalRuntimeConfig{
		Enabled:           false,
		MaxProviderPasses: 30,
		SelfCheckPasses:   2,
		VerifyMaxRetries:  3,
		ErrorMaxRetries:   3,
		ProgressInterval:  5,
	}
}

// goalConfigProvider 由 host 层实现并注入 NewService（resolver 类型断言）。
type goalConfigProvider interface {
	GoalRuntimeConfig() GoalRuntimeConfig
}
```

Modify `internal/backend/forwarder/service.go`:
- 在 Service struct（`:51-78`）加字段：
```go
	goalConfig              goalConfigProvider
	goalsMu                 sync.RWMutex
	goals                   map[string]*GoalState // conversationID → goal 状态，保留最近 100 条
```
- 在 NewService（`:128-210`）里，`delegationConfig` 断言（`:162-165`）之后加：
```go
	var goalCfg goalConfigProvider
	if candidate, ok := resolver.(goalConfigProvider); ok {
		goalCfg = candidate
	}
```
- 在 `service := &Service{...}` 字面量加 `goalConfig: goalCfg, goals: make(map[string]*GoalState),`

Modify `internal/backend/server/config/types.go`：在 `Config` struct（`:149-167`）加：
```go
	// Goal 循环执行（codex-style goal）配置。
	Goal GoalConfig `yaml:"goal" json:"goal"`
```
在 `DefaultConfig()`（`:169-200`）加 `Goal: DefaultGoalConfig(),`；在 `NormalizeConfig()`（`:202-234`）加 `c.Goal = normalizeGoalConfig(c.Goal)`。新增：
```go
type GoalConfig struct {
	Enabled            bool    `yaml:"enabled" json:"enabled"`
	MaxProviderPasses  int     `yaml:"max_provider_passes" json:"maxProviderPasses"`
	MaxDurationSeconds int     `yaml:"max_duration_seconds" json:"maxDurationSeconds"`
	MaxCostUSD         float64 `yaml:"max_cost_usd" json:"maxCostUsd"`
	SelfCheckPasses    int     `yaml:"self_check_passes" json:"selfCheckPasses"`
	VerifyMaxRetries   int     `yaml:"verify_max_retries" json:"verifyMaxRetries"`
	ErrorMaxRetries    int     `yaml:"error_max_retries" json:"errorMaxRetries"`
	ProgressInterval   int     `yaml:"progress_interval" json:"progressInterval"`
}

func DefaultGoalConfig() GoalConfig {
	return GoalConfig{MaxProviderPasses: 30, SelfCheckPasses: 2, VerifyMaxRetries: 3, ErrorMaxRetries: 3, ProgressInterval: 5}
}

func normalizeGoalConfig(c GoalConfig) GoalConfig {
	if c.MaxProviderPasses < 0 {
		c.MaxProviderPasses = 30
	}
	if c.SelfCheckPasses < 1 {
		c.SelfCheckPasses = 2
	}
	if c.VerifyMaxRetries < 0 {
		c.VerifyMaxRetries = 3
	}
	if c.ErrorMaxRetries < 0 {
		c.ErrorMaxRetries = 3
	}
	if c.ProgressInterval < 1 {
		c.ProgressInterval = 5
	}
	return c
}
```

Modify `internal/backend/host.go`：在持有 `host.configs` 的类型上实现 provider（若 `host.configs` 的直接类型已 import forwarder 则加方法；否则在 host.go 定义包装方法）。转换逻辑：
```go
func goalRuntimeConfigFromConfig(c serverconfig.GoalConfig) forwarder.GoalRuntimeConfig {
	return forwarder.GoalRuntimeConfig{
		Enabled:           c.Enabled,
		MaxProviderPasses: c.MaxProviderPasses,
		MaxDuration:       time.Duration(c.MaxDurationSeconds) * time.Second,
		MaxCostUSD:        c.MaxCostUSD,
		SelfCheckPasses:   c.SelfCheckPasses,
		VerifyMaxRetries:  c.VerifyMaxRetries,
		ErrorMaxRetries:   c.ErrorMaxRetries,
		ProgressInterval:  c.ProgressInterval,
	}
}
```
（实现者按 host.go 现有 `DelegationRuntimeConfig` 的注入方式对照完成，保证 resolver 断言 `goalConfigProvider` 成功。默认值兜底：`forwarder.defaultGoalRuntimeConfig()`。）

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/backend/forwarder/ -run 'TestNewGoalState|TestDefaultGoalRuntimeConfig' -v`
Expected: PASS
然后 `go build ./...` 确认配置与 host 改动编译通过。

- [ ] **Step 5: Commit**

```bash
git add internal/backend/forwarder/goal.go internal/backend/forwarder/goal_test.go internal/backend/forwarder/service.go internal/backend/server/config/types.go internal/backend/host.go
git commit -m "feat(goal): 新增 goal 状态类型与运行时配置注入"
```

---

### Task 2: `/goal` 文本命令识别与会话初始化

**Files:**
- Modify: `internal/backend/forwarder/goal.go`（加 `parseGoalCommand`、`registerGoal`）
- Modify: `internal/backend/forwarder/goal_test.go`
- Modify: `internal/backend/forwarder/types.go:518`（InboundIntent 加字段）
- Modify: `internal/backend/forwarder/service.go:709-869`（handleRunIntent 识别 + 初始化）

**Interfaces:**
- Produces: `func parseGoalCommand(text string) (goalText string, strict bool, isGoal bool)`；`func (service *Service) registerGoal(conversationID string, state *GoalState)`；`func (service *Service) GoalSnapshots() []GoalSnapshot`（`GoalSnapshot` 定义见本任务 Step 3，Task 8 的 binding 复用它）。
- Consumes: `InboundIntent`（types.go:518）、`userMessageText`（service.go:3676）。

- [ ] **Step 1: Write the failing test**

Append to `goal_test.go`:

```go
func TestParseGoalCommand(t *testing.T) {
	cases := []struct {
		name       string
		input      string
		wantText   string
		wantStrict bool
		wantGoal   bool
	}{
		{"slash goal", "/goal 修复登录 bug", "修复登录 bug", false, true},
		{"hash goal", "#goal 跑通全部单测", "跑通全部单测", false, true},
		{"uppercase", "/GOAL 重构模块", "重构模块", false, true},
		{"strict", "/goal --strict 实现支付流程", "实现支付流程", true, true},
		{"goal empty", "/goal   ", "", false, false},
		{"no prefix", "修复登录 bug", "", false, false},
		{"prefix in middle", "请 /goal 修复", "", false, false},
		{"goal colon", "goal: 整理依赖", "整理依赖", false, true},
	}
	for _, tc := range cases {
		t.Run(tc.name, func(t *testing.T) {
			gotText, gotStrict, gotGoal := parseGoalCommand(tc.input)
			if gotText != tc.wantText || gotStrict != tc.wantStrict || gotGoal != tc.wantGoal {
				t.Fatalf("parseGoalCommand(%q) = (%q, %v, %v), want (%q, %v, %v)", tc.input, gotText, gotStrict, gotGoal, tc.wantText, tc.wantStrict, tc.wantGoal)
			}
		})
	}
}

func TestGoalSnapshotsEmpty(t *testing.T) {
	service := &Service{goals: make(map[string]*GoalState)}
	snaps := service.GoalSnapshots()
	if len(snaps) != 0 {
		t.Fatalf("expected empty snapshots, got %d", len(snaps))
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/backend/forwarder/ -run 'TestParseGoalCommand|TestGoalSnapshotsEmpty' -v`
Expected: FAIL with "undefined: parseGoalCommand / GoalSnapshots"

- [ ] **Step 3: Write minimal implementation**

Append to `internal/backend/forwarder/goal.go`:

```go
// goalCommandPrefixes 是 /goal 文本命令的识别前缀。命中后前缀与可选的 --strict
// flag 被剥离，剩余文本作为 goal 目标写入 GoalState（Task 8 的前端面板复用同一解析）。
// --strict 借鉴 Reasonix 的 /goal --strict：校验不通过不允许覆盖/兜底。
var goalCommandPrefixes = []string{"/goal", "#goal", "goal:"}

func parseGoalCommand(text string) (goalText string, strict bool, isGoal bool) {
	trimmed := strings.TrimSpace(text)
	lower := strings.ToLower(trimmed)
	for _, prefix := range goalCommandPrefixes {
		if strings.HasPrefix(lower, prefix) {
			rest := strings.TrimSpace(trimmed[len(prefix):])
			if rest == "" {
				return "", false, false
			}
			restLower := strings.ToLower(rest)
			if strings.HasPrefix(restLower, "--strict") {
				rest = strings.TrimSpace(rest[len("--strict"):])
				if rest == "" {
					return "", true, false
				}
				return rest, true, true
			}
			return rest, false, true
		}
	}
	return "", false, false
}

// GoalSnapshot 是暴露给前端（wails binding）的稳定快照。
type GoalSnapshot struct {
	ConversationID string  `json:"conversationId"`
	GoalText       string  `json:"goalText"`
	Status         string  `json:"status"`
	ProviderPasses int     `json:"providerPasses"`
	ToolCalls      int     `json:"toolCalls"`
	SelfChecks     int     `json:"selfChecks"`
	RetryCount     int     `json:"retryCount"`
	CostEstimateUSD float64 `json:"costEstimateUsd"`
	LastProgress   string  `json:"lastProgress"`
	CompletionText string  `json:"completionText"`
	StopReason     string  `json:"stopReason"`
	StartedAt      string  `json:"startedAt"`
	UpdatedAt      string  `json:"updatedAt"`
}

// registerGoal 登记 goal 状态（内存 map，保留最近 100 条）。
func (service *Service) registerGoal(conversationID string, state *GoalState) {
	if service == nil || state == nil {
		return
	}
	service.goalsMu.Lock()
	defer service.goalsMu.Unlock()
	service.goals[conversationID] = state
	if len(service.goals) > 100 {
		// 淘汰最旧一条
		var oldestKey string
		var oldest time.Time
		for k, v := range service.goals {
			if oldestKey == "" || v.UpdatedAt.Before(oldest) {
				oldestKey, oldest = k, v.UpdatedAt
			}
		}
		if oldestKey != "" {
			delete(service.goals, oldestKey)
		}
	}
}

// GoalSnapshots 返回全部已登记 goal 的稳定快照（供前端面板轮询）。
func (service *Service) GoalSnapshots() []GoalSnapshot {
	if service == nil {
		return nil
	}
	service.goalsMu.RLock()
	defer service.goalsMu.RUnlock()
	snaps := make([]GoalSnapshot, 0, len(service.goals))
	for _, state := range service.goals {
		snaps = append(snaps, goalSnapshotOf(state))
	}
	return snaps
}

func goalSnapshotOf(state *GoalState) GoalSnapshot {
	return GoalSnapshot{
		ConversationID: state.ConversationID,
		GoalText:       state.GoalText,
		Status:         string(state.Status),
		ProviderPasses: state.ProviderPasses,
		ToolCalls:      state.ToolCalls,
		SelfChecks:     state.SelfChecks,
		RetryCount:     state.RetryCount,
		CostEstimateUSD: state.CostEstimateUSD,
		LastProgress:   state.LastProgress,
		CompletionText: state.CompletionText,
		StopReason:     state.StopReason,
		StartedAt:      state.StartedAt.Format(time.RFC3339),
		UpdatedAt:      state.UpdatedAt.Format(time.RFC3339),
	}
}
```

Modify `internal/backend/forwarder/types.go` 的 `InboundIntent`（`:518` 附近）加字段：
```go
	// GoalMode 标记该 run 以 goal 模式执行（/goal 前缀或前端面板发起）。
	GoalMode   bool
	GoalText   string
	GoalStrict bool // /goal --strict 标记（借鉴 Reasonix Strict 模式）
```

Modify `internal/backend/forwarder/service.go` `handleRunIntent`：
- 在函数开头（`:709-717` 之间，`normalizeUserMessageForStorage` 之前）加：
```go
	if !intent.GoalMode {
		if goalText, strict, isGoal := parseGoalCommand(userMessageText(intent.UserMessage)); isGoal {
			intent.GoalMode = true
			intent.GoalText = goalText
			intent.GoalStrict = strict
			// 剥离前缀，避免 goal 目标文本被当作指令重复注入。
			intent.UserMessage = replaceUserMessageText(intent.UserMessage, goalText)
		}
	}
```
- 在 `broker.OpenStream(...)`（`:784`）之后、`replaceCheckpointConversation` 之前加：
```go
	if intent.GoalMode {
		goalState := newGoalState(intent.ConversationID, intent.GoalText, intent.GoalStrict)
		stream.Goal = goalState
		service.registerGoal(intent.ConversationID, goalState)
		service.debug.LogRuntime(context.Background(), intent.RequestID, intent.ConversationID, "goal_started", map[string]any{
			"goal_text": intent.GoalText,
		})
	}
```
- 新增辅助函数（放 `goal.go`）：
```go
// replaceUserMessageText 返回替换 text 后的 UserMessage 副本。
func replaceUserMessageText(message *agentv1.UserMessage, text string) *agentv1.UserMessage {
	if message == nil {
		return &agentv1.UserMessage{Text: text}
	}
	cloned := proto.Clone(message).(*agentv1.UserMessage)
	cloned.Text = text
	return cloned
}
```
（需在 goal.go 头部 import `agentv1 "cursor/gen/agentv1"` 与 `"google.golang.org/protobuf/proto"`；若 `agentv1.UserMessage` 是普通 struct 可直接拷贝，实现者以实际生成代码为准。）

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/backend/forwarder/ -run 'TestParseGoalCommand|TestGoalSnapshotsEmpty' -v`
Expected: PASS；然后 `go build ./...` 确认 InboundIntent/UserMessage 改动编译通过。

- [ ] **Step 5: Commit**

```bash
git add internal/backend/forwarder/goal.go internal/backend/forwarder/goal_test.go internal/backend/forwarder/types.go internal/backend/forwarder/service.go
git commit -m "feat(goal): /goal 文本命令识别与 goal 会话初始化"
```

---

### Task 3: goal system prompt 注入

**Files:**
- Modify: `internal/backend/forwarder/goal.go`（加 `goalSystemPromptFragment`、`joinNonEmpty`）
- Modify: `internal/backend/forwarder/goal_test.go`
- Modify: `internal/backend/forwarder/service.go:1735`（driveProvider Compile 调用点）

**Interfaces:**
- Produces: `func goalSystemPromptFragment(goal *GoalState, cfg GoalRuntimeConfig) string`；`func joinNonEmpty(parts ...string) string`。
- Consumes: `GoalState`（Task 1）、`GoalRuntimeConfig`（Task 1）。编译点：`service.compiler.Compile(conversation, mode, latestUserText, modelName, stream.CustomSystemPrompt)`（service.go:1735，goal 指令追加在 customSystemPrompt 位置，保持前缀稳定）。

- [ ] **Step 1: Write the failing test**

Append to `goal_test.go`:

```go
func TestGoalSystemPromptFragment(t *testing.T) {
	goal := &GoalState{GoalText: "修复全部测试"}
	cfg := defaultGoalRuntimeConfig()
	cfg.MaxProviderPasses = 10
	frag := goalSystemPromptFragment(goal, cfg)
	for _, want := range []string{"GOAL", "修复全部测试", "10 轮", "自检", "失败", "完成报告"} {
		if !strings.Contains(frag, want) {
			t.Fatalf("fragment missing %q: %s", want, frag)
		}
	}
	if frag := goalSystemPromptFragment(nil, cfg); frag != "" {
		t.Fatalf("nil goal must produce empty fragment, got %q", frag)
	}
	if frag := goalSystemPromptFragment(&GoalState{GoalText: "  "}, cfg); frag != "" {
		t.Fatalf("blank goal must produce empty fragment, got %q", frag)
	}
}

func TestJoinNonEmpty(t *testing.T) {
	if got := joinNonEmpty("a", "", "b", "  "); got != "a\n\nb" {
		t.Fatalf("joinNonEmpty = %q, want %q", got, "a\n\nb")
	}
	if got := joinNonEmpty("", ""); got != "" {
		t.Fatalf("joinNonEmpty all empty = %q, want empty", got)
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/backend/forwarder/ -run 'TestGoalSystemPromptFragment|TestJoinNonEmpty' -v`
Expected: FAIL with "undefined: goalSystemPromptFragment / joinNonEmpty"

- [ ] **Step 3: Write minimal implementation**

Append to `internal/backend/forwarder/goal.go`:

```go
// goalSystemPromptFragment 生成 goal 模式的系统指令段，追加在
// customSystemPrompt 位置（systemParts 末尾），避免影响前缀稳定性。
func goalSystemPromptFragment(goal *GoalState, cfg GoalRuntimeConfig) string {
	if goal == nil || strings.TrimSpace(goal.GoalText) == "" {
		return ""
	}
	parts := []string{
		"你当前处于 GOAL 模式。你的目标（GOAL）：",
		goal.GoalText,
		"",
		"执行要求：",
		"0. 先拆解目标：把目标拆成 3-8 个可验证的步骤清单（在回复中列出），逐项完成并标记。",
		"1. 自主决策：持续调用工具推进目标，不要轻易停下或询问用户；只有真正无法继续时才停下并说明卡点。",
		"2. 循环执行：一轮结束（没有工具调用）不代表完成。请自检目标是否达成：未达成则继续执行下一轮，直到目标真正达成。",
		"3. 失败重试：工具执行失败时分析原因、换一种方式重试，不要直接放弃。",
		"4. 进度汇报：每完成一个阶段，用简短一句话汇报当前进度。",
		"5. 完成标准：目标的所有要求都满足后才算完成。完成时输出以 [goal:complete] 开头（单独一行）的最终完成报告，说明你做了什么、验证了什么、结果如何；不要在没有真正完成时输出该标记。",
	}
	if cfg.MaxProviderPasses > 0 {
		parts = append(parts, fmt.Sprintf("6. 预算：本 goal 最多执行 %d 轮 provider 调用，请高效推进，优先完成最关键步骤。", cfg.MaxProviderPasses))
	}
	return strings.Join(parts, "\n")
}

// joinNonEmpty 用空行连接非空片段。
func joinNonEmpty(parts ...string) string {
	var nonEmpty []string
	for _, p := range parts {
		if strings.TrimSpace(p) != "" {
			nonEmpty = append(nonEmpty, strings.TrimSpace(p))
		}
	}
	return strings.Join(nonEmpty, "\n\n")
}
```
（goal.go 头部补 import `"fmt"`。）

Modify `internal/backend/forwarder/service.go:1735`：把
```go
	compiled, err := service.compiler.Compile(conversation, mode, latestUserText, modelName, stream.CustomSystemPrompt)
```
改为
```go
	customSystemPrompt := stream.CustomSystemPrompt
	if stream.Goal != nil {
		if frag := goalSystemPromptFragment(stream.Goal, service.currentGoalConfig()); frag != "" {
			customSystemPrompt = joinNonEmpty(customSystemPrompt, frag)
		}
	}
	compiled, err := service.compiler.Compile(conversation, mode, latestUserText, modelName, customSystemPrompt)
```
并新增：
```go
// currentGoalConfig 返回 goal 运行时配置；provider 未注入时用默认值。
func (service *Service) currentGoalConfig() GoalRuntimeConfig {
	if service == nil || service.goalConfig == nil {
		return defaultGoalRuntimeConfig()
	}
	return service.goalConfig.GoalRuntimeConfig()
}
```
（`driveProvider` 已在 `stream.mu` 释放后读取 stream 字段，此处 `stream.Goal` 需在 `:1668-1709` 的锁内一并读出到局部变量，避免数据竞争——实现者把 `goal := stream.Goal` 加入锁内局部变量列表，`stream.Goal` 之后只用局部变量 `goal`。）

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/backend/forwarder/ -run 'TestGoalSystemPromptFragment|TestJoinNonEmpty' -v`
Expected: PASS；`go build ./...` 通过；`go test -race ./internal/backend/forwarder/ -run TestNothing` 无 race 报告（若跑全量太慢可省略，改为人工检查锁内读取）。

- [ ] **Step 5: Commit**

```bash
git add internal/backend/forwarder/goal.go internal/backend/forwarder/goal_test.go internal/backend/forwarder/service.go
git commit -m "feat(goal): goal 模式系统指令注入"
```

---

### Task 4: goal 循环续跑——预算检查与自检提示

**Files:**
- Modify: `internal/backend/forwarder/goal.go`（加 `goalBudgetExceeded`、`handleGoalPassFinished`、`appendGoalPromptContext`、`goalIdleReminder`、`goalVerifyFeedbackReminder`、常量）
- Modify: `internal/backend/forwarder/goal_test.go`
- Modify: `internal/backend/forwarder/actor.go:1010-1012`（handleProviderDoneEvent 收尾前接线）
- Modify: `internal/backend/forwarder/prompt_context.go`（加 source 常量，若该文件已有常量区则并入）

**Interfaces:**
- Produces: `func goalBudgetExceeded(goal *GoalState, cfg GoalRuntimeConfig) (bool, string)`；`func (service *Service) handleGoalPassFinished(stream *ActiveStream, conversationID string, turnSeq int64, requestID, modelCallID string, providerPass int, finishReason, accumulatedText string, hadToolInvocation bool) (bool, error)`；`const goalCompletionMarker = "[goal:complete]"`；`const promptContextSourceGoalIdle = "goal_idle"`；`func goalIdleReminder(goal *GoalState, idleCount int) PromptContextMessage`；`func goalVerifyFeedbackReminder(goal *GoalState, feedback string) PromptContextMessage`；`const goalStalePivotThreshold = 2`。
- Consumes: `newPromptContextEntry`（prompt_context.go:117）、`service.appendConversationEntries`（actor_recovery.go:45 的用法）、`service.requestProviderAction`、`service.publishCheckpoint`、`service.syncSummaryCarryForward`。
- 校验子代理 `goalVerifyCompletion` 在 Task 5 实现，本任务先以 `service.goalVerifyCompletion` 为可调用目标（Task 5 填充），本任务 Step 3 先声明 stub 返回 `(false, "", nil)` 以通过编译——不，writing-plans 禁止占位：改为本任务 handleGoalPassFinished 里"到达自检阈值"分支直接调用 Task 5 将实现的 `service.goalVerifyCompletion`，并让 Task 5 在本任务之后立即填充实现；两个任务同批提交前必须 `go build ./...` 通过。

- [ ] **Step 1: Write the failing test**

Append to `goal_test.go`:

```go
func TestGoalBudgetExceeded(t *testing.T) {
	cfg := defaultGoalRuntimeConfig()
	cfg.MaxProviderPasses = 5
	goal := &GoalState{ProviderPasses: 4}
	if exceeded, _ := goalBudgetExceeded(goal, cfg); exceeded {
		t.Fatal("pass 4 of 5 must not exceed")
	}
	goal.ProviderPasses = 5
	if exceeded, reason := goalBudgetExceeded(goal, cfg); !exceeded || reason == "" {
		t.Fatalf("pass 5 of 5 must exceed, got exceeded=%v reason=%q", exceeded, reason)
	}
	unlimited := defaultGoalRuntimeConfig()
	goal.ProviderPasses = 999
	if exceeded, _ := goalBudgetExceeded(goal, unlimited); exceeded {
		t.Fatal("unlimited passes must never exceed")
	}
	durCfg := defaultGoalRuntimeConfig()
	durCfg.MaxDuration = time.Minute
	old := &GoalState{StartedAt: time.Now().UTC().Add(-2 * time.Minute), ProviderPasses: 1}
	if exceeded, _ := goalBudgetExceeded(old, durCfg); !exceeded {
		t.Fatal("started 2m ago with 1m budget must exceed")
	}
}

func TestGoalIdleReminder(t *testing.T) {
	goal := &GoalState{GoalText: "修复测试"}
	msg := goalIdleReminder(goal, 1)
	if msg.Source != promptContextSourceGoalIdle {
		t.Fatalf("source = %q", msg.Source)
	}
	if !strings.Contains(msg.Message.Content, "修复测试") {
		t.Fatalf("reminder missing goal text: %q", msg.Message.Content)
	}
	if !strings.Contains(msg.Message.Content, "连续 1 轮") {
		t.Fatalf("reminder missing idle count: %q", msg.Message.Content)
	}
	escalated := goalIdleReminder(goal, goalStalePivotThreshold)
	if !strings.Contains(escalated.Message.Content, "换策略") {
		t.Fatalf("escalated reminder missing pivot instruction: %q", escalated.Message.Content)
	}
}

func TestGoalVerifyFeedbackReminder(t *testing.T) {
	goal := &GoalState{GoalText: "修复测试"}
	msg := goalVerifyFeedbackReminder(goal, "仍有 3 个用例失败")
	if msg.Source != promptContextSourceGoalVerifyFeedback {
		t.Fatalf("source = %q", msg.Source)
	}
	if !strings.Contains(msg.Message.Content, "仍有 3 个用例失败") {
		t.Fatalf("feedback missing report: %q", msg.Message.Content)
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/backend/forwarder/ -run 'TestGoalBudgetExceeded|TestGoalIdleReminder|TestGoalVerifyFeedbackReminder' -v`
Expected: FAIL with "undefined: goalBudgetExceeded / goalIdleReminder"

- [ ] **Step 3: Write minimal implementation**

Append to `internal/backend/forwarder/goal.go`:

```go
// goalBudgetExceeded 检查 goal 是否超出预算（passes / 时长）。
// 返回 (是否超限, 原因)。MaxCostUSD 的费用检查在 handleGoalPassFinished 里
// 依赖 cost 估算结果（见 Task 4 Step 3 的 updateGoalCostEstimate）。
func goalBudgetExceeded(goal *GoalState, cfg GoalRuntimeConfig) (bool, string) {
	if goal == nil {
		return false, ""
	}
	if cfg.MaxProviderPasses > 0 && goal.ProviderPasses >= cfg.MaxProviderPasses {
		return true, fmt.Sprintf("达到 provider pass 上限 %d", cfg.MaxProviderPasses)
	}
	if cfg.MaxDuration > 0 && !goal.StartedAt.IsZero() && time.Since(goal.StartedAt) >= cfg.MaxDuration {
		return true, fmt.Sprintf("达到时长上限 %s", cfg.MaxDuration)
	}
	return false, ""
}

// goalCompletionMarker 是模型声明"目标已完成"的显式标记。借鉴 Reasonix 的
// [goal:complete] 完成声明拦截：后端不轻信"无工具调用"，只认显式声明；
// 声明后仍需校验子代理证据审计才放行（非 strict 且无校验能力时退化为自检）。
const goalCompletionMarker = "[goal:complete]"

// goalStalePivotThreshold 是校验连续未通过 / 连续停顿达到该次数后，提示结构性
// 换策略的阈值。借鉴 Reasonix AutoResearch 的 stale_count pivot：换入口点、
// 任务分解或验证方式，而不是重复同一做法。
const goalStalePivotThreshold = 2

const promptContextSourceGoalIdle = "goal_idle"
const promptContextSourceGoalVerifyFeedback = "goal_verify_feedback"
const promptContextSourceGoalErrorRetry = "goal_error_retry"
const promptContextSourceGoalBudget = "goal_budget"

// goalIdleReminder 是停顿提醒：无工具调用且未声明完成时，要求模型继续推进
// 或说明卡点；连续多轮停顿则要求改变策略（文案随 idleCount 升级）。
func goalIdleReminder(goal *GoalState, idleCount int) PromptContextMessage {
	body := fmt.Sprintf("目标：%s\n\n检测到你已连续 %d 轮没有调用工具。若目标尚未达成，请继续调用工具执行；若遇到卡点，请明确说明卡点并尝试换一种方式突破。", goal.GoalText, idleCount)
	if idleCount >= goalStalePivotThreshold {
		body += "\n连续多轮无进展：请结构性换策略（改变入口点、任务分解或验证方式），不要重复同样的做法。"
	}
	return newPromptContextReminder(promptContextSourceGoalIdle, body)
}

// goalVerifyFeedbackReminder 是校验未通过反馈：列出校验子代理的未达成理由，
// 要求继续执行（Reasonix goal intercept 的"列出未完成项"角色）。
func goalVerifyFeedbackReminder(goal *GoalState, feedback string) PromptContextMessage {
	body := fmt.Sprintf("目标：%s\n\n校验子代理判定目标尚未达成，理由：%s\n请根据该反馈继续执行，直到目标真正达成。", goal.GoalText, feedback)
	return newPromptContextReminder(promptContextSourceGoalVerifyFeedback, body)
}

// appendGoalPromptContext 追加一条 goal 提示到 conversation 并落库（幂等：同一
// source 与 turn 去重，避免无限注入）。返回是否实际追加。
func (service *Service) appendGoalPromptContext(stream *ActiveStream, conversationID string, turnSeq int64, requestID, source, text string) (bool, error) {
	if service == nil || stream == nil || strings.TrimSpace(text) == "" {
		return false, nil
	}
	conversation, _, _, err := service.snapshotCheckpointConversation(stream)
	if err != nil {
		return false, err
	}
	if conversation == nil {
		return false, nil
	}
	if currentTurnHasPromptContextSource(conversation, turnSeq, source) {
		return false, nil
	}
	msg := PromptContextMessage{
		Source:  source,
		Message: newPromptContextText(text),
	}
	if _, err := service.appendConversationEntries(stream, conversationID, []HistoryEntry{
		newPromptContextEntry(turnSeq, requestID, msg),
	}); err != nil {
		return false, err
	}
	return true, nil
}

// handleGoalPassFinished 在 provider pass 无工具调用收尾点接管 goal 循环：
// 返回 (true, nil) 表示已挂起继续（调用方 return）；(false, nil) 表示走正常收口
// （完成 / 预算超限 / 失败）；(false, err) 表示循环级错误。
func (service *Service) handleGoalPassFinished(stream *ActiveStream, conversationID string, turnSeq int64, requestID, modelCallID string, providerPass int, finishReason, accumulatedText string, hadToolInvocation bool) (bool, error) {
	goal := stream.Goal
	if goal == nil {
		return false, nil
	}
	goal.ProviderPasses = providerPass
	goal.ToolCalls += stream.ToolInvocationCount
	goal.UpdatedAt = time.Now().UTC()

	cfg := service.currentGoalConfig()

	// 预算检查（含费用估算：本 pass 的 token 用量计入累计费用后检查 MaxCostUSD）。
	service.updateGoalCostEstimate(stream, goal)
	if exceeded, reason := goalBudgetExceeded(goal, cfg); exceeded {
		goal.Status = GoalStatusBudgetExceeded
		goal.StopReason = reason
		goal.CompletionText = fmt.Sprintf("goal 因预算上限停止：%s", reason)
		service.emitGoalCompletion(stream, goal, "budget")
		return false, nil
	}
	if cfg.MaxCostUSD > 0 && goal.CostEstimateUSD >= cfg.MaxCostUSD {
		goal.Status = GoalStatusBudgetExceeded
		goal.StopReason = fmt.Sprintf("达到费用上限 $%.4f", cfg.MaxCostUSD)
		goal.CompletionText = goal.StopReason
		service.emitGoalCompletion(stream, goal, "budget")
		return false, nil
	}

	// 有工具调用：现有循环逻辑（actor.go:1012）继续 resume，这里让行。
	if hadToolInvocation || shouldResumeAfterToolResults(finishReason) {
		return false, nil
	}

	// 无工具调用：模型要么显式声明完成（[goal:complete]），要么停顿。
	// 借鉴 Reasonix 的完成声明拦截：不轻信"无工具调用"，只认显式声明；
	// 停顿则注入 idle 提醒（连续多轮升级为换策略提示）。
	goal.consecutiveIdle++
	goal.LastProgress = truncateText(accumulatedText, 120)
	completionClaimed := strings.Contains(strings.ToLower(accumulatedText), goalCompletionMarker)

	if completionClaimed {
		goal.CompletionClaimed = true
		verified, report, err := service.goalVerifyCompletion(stream, goal)
		if err != nil {
			return false, err
		}
		if verified {
			goal.Status = GoalStatusCompleted
			goal.CompletionText = truncateText(report, 2000)
			goal.StopReason = ""
			service.emitGoalCompletion(stream, goal, "completed")
			return false, nil
		}
		if goal.RetryCount >= cfg.VerifyMaxRetries {
			goal.Status = GoalStatusFailed
			goal.StopReason = fmt.Sprintf("校验子代理连续 %d 次判定目标未达成", goal.RetryCount)
			goal.CompletionText = truncateText(report, 2000)
			service.emitGoalCompletion(stream, goal, "failed")
			return false, nil
		}
		goal.RetryCount++
		goal.consecutiveIdle = 0
		feedback := report
		if goal.RetryCount >= goalStalePivotThreshold {
			goal.StaleCount++
			feedback = fmt.Sprintf("%s\n\n已连续 %d 次未通过校验：请结构性换策略（改变入口点、任务分解或验证方式），不要重复同一做法。", feedback, goal.StaleCount)
		}
		appended, err := service.appendGoalPromptContext(stream, conversationID, turnSeq, requestID, promptContextSourceGoalVerifyFeedback, goalVerifyFeedbackReminder(goal, feedback).Message.Content)
		if err != nil {
			return false, err
		}
		if !appended {
			return false, nil // 已注入过，避免死循环，走收口
		}
		service.emitGoalProgress(stream, goal)
		if err := service.syncSummaryCarryForward(conversationID, requestID, modelCallID); err != nil {
			return true, err
		}
		if err := service.publishCheckpoint(requestID, conversationID); err != nil {
			return true, err
		}
		return true, service.requestProviderAction(stream, providerActionResume)
	}

	appended, err := service.appendGoalPromptContext(stream, conversationID, turnSeq, requestID, promptContextSourceGoalIdle, goalIdleReminder(goal, goal.consecutiveIdle).Message.Content)
	if err != nil {
		return false, err
	}
	if !appended {
		return false, nil
	}
	// 每 ProgressInterval 个 pass 推一次进度摘要。
	if cfg.ProgressInterval > 0 && goal.ProviderPasses%cfg.ProgressInterval == 0 {
		service.emitGoalProgress(stream, goal)
	}
	if err := service.syncSummaryCarryForward(conversationID, requestID, modelCallID); err != nil {
		return true, err
	}
	if err := service.publishCheckpoint(requestID, conversationID); err != nil {
		return true, err
	}
	return true, service.requestProviderAction(stream, providerActionResume)
}
```

配套辅助（也放 goal.go，Task 5 使用）：
```go
// updateGoalCostEstimate 用本 pass 的 token 用量估算费用并累加。
// 没有 pricing provider 时保持 0（费用检查自然跳过）。Task 5 实现具体估算。
func (service *Service) updateGoalCostEstimate(stream *ActiveStream, goal *GoalState) {
	if service == nil || stream == nil || goal == nil || service.usageCostEstimator == nil {
		return
	}
	cost, ok := service.usageCostEstimator.Cost(stream, stream.ProviderUsage)
	if ok {
		goal.CostEstimateUSD += cost
	}
}
```
（`usageCostEstimator` 接口与实现见 Task 5；本任务先给 `Service` 加字段并在 `updateGoalCostEstimate` 判空，`service.usageCostEstimator` 未赋值时直接 return，不影响本任务编译。）

Modify `internal/backend/forwarder/prompt_context.go`：确认 `newPromptContextText` 与 `PromptContextMessage` 可用（`prompt_context.go:44` 的 `newPromptContextReminder` 与 `normalizePromptContextMessage`；若 `newPromptContextText` 不存在，用现有 `newPromptContextMessage(content string)` 或等价构造，实现者以文件内现有构造函数为准）。

Modify `internal/backend/forwarder/actor.go` `handleProviderDoneEvent`：在 `:1010`（existingCompletion 分支结束）之后、`:1012`（`hadToolInvocation || shouldResumeAfterToolResults`）之前插入：
```go
	// goal 模式：无工具调用不直接收口，先做目标自检 / 预算判定 / 校验。
	if stream.Goal != nil {
		handled, err := service.handleGoalPassFinished(stream, conversationID, turnSeq, requestID, modelCallID, providerPass, finishReason, accumulatedText, hadToolInvocation)
		if err != nil {
			return service.failStreamIfNonTerminal(stream, "unknown", err)
		}
		if handled {
			return nil
		}
	}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/backend/forwarder/ -run 'TestGoalBudgetExceeded|TestGoalIdleReminder|TestGoalVerifyFeedbackReminder' -v`
Expected: PASS；`go build ./...` 通过（本任务引用的 `goalVerifyCompletion` / `usageCostEstimator` 未实现——为保持可编译，Task 5 必须紧接实现；若按任务顺序单跑本任务构建失败，允许先提交本任务纯函数（Step 1-2 的测试 + budget/reminder 实现），循环接线部分在 Task 5 完成后一并编译验证）。

- [ ] **Step 5: Commit**

```bash
git add internal/backend/forwarder/goal.go internal/backend/forwarder/goal_test.go internal/backend/forwarder/actor.go internal/backend/forwarder/prompt_context.go
git commit -m "feat(goal): goal 循环预算检查与自检提示续跑"
```

---

### Task 5: 校验子代理完成判定 + 费用估算

**Files:**
- Modify: `internal/backend/forwarder/goal.go`（加 `goalVerifyCompletion`、`goalVerifyPrompt`、`parseVerifyDecision`、`goalUsageCostEstimator` 接口与默认实现）
- Modify: `internal/backend/forwarder/goal_test.go`
- Modify: `internal/backend/forwarder/service.go`（Service struct 加 `usageCostEstimator` 字段，NewService 里用默认实现赋值）

**Interfaces:**
- Produces: `func (service *Service) goalVerifyCompletion(stream *ActiveStream, goal *GoalState) (verified bool, report string, err error)`；`func parseVerifyDecision(output string) (verified bool, report string)`；`func goalVerifyPrompt(goal *GoalState) string`；`type goalUsageCostEstimator interface { Cost(stream *ActiveStream, usage turnUsageSnapshot) (float64, bool) }`。
- Consumes: `service.multitaskDelegation.scheduler`（delegation_multitask.go:85，`*delegation.Scheduler`）、`delegation.TaskRequest`（delegation/scheduler.go:40）、`delegation.SupervisionTaskContract`（delegation/supervision.go:71，字段 `DoneCriteria []string`）、`Scheduler.Submit/WaitForTerminal/Result`（scheduler.go:207/667/593）。

- [ ] **Step 1: Write the failing test**

Append to `goal_test.go`:

```go
func TestParseVerifyDecision(t *testing.T) {
	cases := []struct {
		name       string
		output     string
		wantVer    bool
		wantReport string
	}{
		{"verified", "VERIFIED\n测试全部通过，无回归。", true, "测试全部通过，无回归。"},
		{"not verified", "NOT_VERIFIED\n仍有 3 个用例失败。", false, "仍有 3 个用例失败。"},
		{"lowercase", "verified\nok", true, "ok"},
		{"blank", "", false, ""},
		{"no marker", "测试全部通过", false, "测试全部通过"},
	}
	for _, tc := range cases {
		t.Run(tc.name, func(t *testing.T) {
			gotVer, gotReport := parseVerifyDecision(tc.output)
			if gotVer != tc.wantVer || gotReport != tc.wantReport {
				t.Fatalf("parseVerifyDecision(%q) = (%v, %q), want (%v, %q)", tc.output, gotVer, gotReport, tc.wantVer, tc.wantReport)
			}
		})
	}
}

func TestGoalVerifyPrompt(t *testing.T) {
	p := goalVerifyPrompt(&GoalState{GoalText: "跑通全部单测"})
	for _, want := range []string{"VERIFIED", "NOT_VERIFIED", "跑通全部单测", "只读"} {
		if !strings.Contains(p, want) {
			t.Fatalf("verify prompt missing %q", want)
		}
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/backend/forwarder/ -run 'TestParseVerifyDecision|TestGoalVerifyPrompt' -v`
Expected: FAIL with "undefined: parseVerifyDecision / goalVerifyPrompt"

- [ ] **Step 3: Write minimal implementation**

Append to `internal/backend/forwarder/goal.go`:

```go
// goalVerifyPrompt 构造校验子代理的任务提示：只读检查目标是否真正达成，
// 输出必须以 VERIFIED / NOT_VERIFIED 开头。
func goalVerifyPrompt(goal *GoalState) string {
	return fmt.Sprintf(`你是只读校验子代理。请检查以下 GOAL 是否已经真正达成：

GOAL：%s

检查要求：
1. 只读检查代码/结果/证据，不要修改任何文件。
2. 逐项核对 GOAL 的要求是否全部满足；检查是否有遗漏、假完成或未验证的部分。
3. 输出格式（严格）：
   第一行：VERIFIED 或 NOT_VERIFIED
   其余行：简短理由（验证了什么、还差什么）。

请基于真实证据判断，不要轻信主代理的自我声明。`, goal.GoalText)
}

// parseVerifyDecision 解析校验子代理输出首行判定。
func parseVerifyDecision(output string) (verified bool, report string) {
	lines := strings.SplitN(strings.TrimSpace(output), "\n", 2)
	if len(lines) == 0 {
		return false, ""
	}
	head := strings.ToUpper(strings.TrimSpace(lines[0]))
	switch head {
	case "VERIFIED":
		verified = true
	case "NOT_VERIFIED":
		verified = false
	default:
		// 未按格式输出时保守判定为未通过，并把全文作为理由。
		return false, strings.TrimSpace(output)
	}
	report = ""
	if len(lines) == 2 {
		report = strings.TrimSpace(lines[1])
	}
	return verified, report
}

// goalVerifyCompletion 同步跑一个只读校验子代理确认 goal 是否达成。
// service.multitaskDelegation 不可用时退化为"模型自检通过"（返回 verified=true），
// 并在日志中说明——保证 goal 在无委派配置时仍能收口而不是卡死。
func (service *Service) goalVerifyCompletion(stream *ActiveStream, goal *GoalState) (bool, string, error) {
	if service == nil || stream == nil || goal == nil {
		return false, "", nil
	}
	if service.multitaskDelegation == nil || service.multitaskDelegation.scheduler == nil {
		// strict 模式不兜底：没有真实校验子代理就按"未通过"处理（借鉴 Reasonix
		// Strict 模式不允许覆盖拦截）；普通模式退化为模型自检通过，保证可收口。
		if goal.Strict {
			return false, "（strict 模式要求真实校验子代理，但委派不可用）", nil
		}
		log.Printf("forwarder goal verify skipped (delegation unavailable) request_id=%s", strings.TrimSpace(stream.RequestID))
		return true, "（无校验子代理可用，采用模型自检结论）", nil
	}
	taskID := "goal-verify-" + uuid.NewString()
	_, err := service.multitaskDelegation.scheduler.Submit(delegation.TaskRequest{
		ID:             taskID,
		ParentRequest:  stream.RequestID,
		ConversationID: stream.ConversationID,
		SubagentType:   "generalPurpose",
		Readonly:       true,
		Prompt:         goalVerifyPrompt(goal),
		Description:    "goal 完成校验",
		Mode:           agentv1.AgentMode_AGENT_MODE_AGENT,
		Timeout:        3 * time.Minute,
		Contract: &delegation.SupervisionTaskContract{
			DoneCriteria: []string{"输出 VERIFIED 或 NOT_VERIFIED 结论与理由"},
		},
	})
	if err != nil {
		return false, "", err
	}
	ctx, cancel := context.WithTimeout(context.Background(), 3*time.Minute)
	defer cancel()
	if err := service.multitaskDelegation.scheduler.WaitForTerminal(ctx, []string{taskID}); err != nil {
		return false, "", err
	}
	result, ok := service.multitaskDelegation.scheduler.Result(taskID)
	if !ok {
		return false, "", fmt.Errorf("goal verify task %s has no result", taskID)
	}
	return parseVerifyDecision(result.Output)
}

// goalUsageCostEstimator 估算单个 provider pass 的美元费用；返回 ok=false 表示
// 无定价来源（费用检查跳过）。
type goalUsageCostEstimator interface {
	Cost(stream *ActiveStream, usage turnUsageSnapshot) (float64, bool)
}

// defaultUsageCostEstimator 用 historymetrics 定价表估算；无表时返回 ok=false。
// NewService 里始终赋值（nil 安全），保证 updateGoalCostEstimate 恒可调用。
type defaultUsageCostEstimator struct{ lookup *historymetrics.PriceLookup }

func (e *defaultUsageCostEstimator) Cost(stream *ActiveStream, usage turnUsageSnapshot) (float64, bool) {
	if e == nil || e.lookup == nil || stream == nil || !usage.UsagePresent {
		return 0, false
	}
	cost, ok, _, _ := e.lookup.Cost(stream.ModelName, usage.Provider, usage.BaseURL, usage.InputTokens, usage.OutputTokens, usage.CacheReadTokens, usage.CacheWriteTokens)
	if !ok || cost == nil {
		return 0, false
	}
	return *cost, true
}
```
（imports 新增：`"context"`、`"cursor/internal/backend/delegation"`、`"cursor/internal/historymetrics"`、`"github.com/google/uuid"`、`agentv1 "cursor/gen/agentv1"`；若 `uuid.NewString` 不在本包 import 集，用现有 `uuid` 包路径，service.go 已有 `uuid.NewString()` 用法。）

Modify `internal/backend/forwarder/service.go`：
- Service struct 加字段：`usageCostEstimator goalUsageCostEstimator`（默认 nil；`updateGoalCostEstimate` 判空保护）。
- NewService 里（`service := &Service{...}`）加 `usageCostEstimator: &defaultUsageCostEstimator{lookup: newPricingLookupFromConfig(resolver)},`。`newPricingLookupFromConfig` 实现：
```go
// newPricingLookupFromConfig 从 resolver 提供的 pricing 配置构建定价表；
// 拿不到时返回 nil（费用估算与检查自动跳过）。
func newPricingLookupFromConfig(resolver modeladapter.ChannelResolver) *historymetrics.PriceLookup {
	provider, ok := resolver.(interface{ PricingRates() []historymetrics.PriceRate })
	if !ok {
		return nil
	}
	return historymetrics.NewPriceLookup(provider.PricingRates())
}
```
（实现者按 host.configs 实际暴露的 pricing 来源对接；若没有现成的 `PricingRates()` 方法，参考 `internal/historymetrics` 里调用 `NewPriceLookup` 的位置从等价来源构建，或在 resolver 上新增该方法返回 `historymetrics.PriceLookup`。）

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/backend/forwarder/ -run 'TestParseVerifyDecision|TestGoalVerifyPrompt' -v`
Expected: PASS；`go build ./...` 通过（Task 4 引用的 `goalVerifyCompletion` / `updateGoalCostEstimate` 现在闭合）。

- [ ] **Step 5: Commit**

```bash
git add internal/backend/forwarder/goal.go internal/backend/forwarder/goal_test.go internal/backend/forwarder/service.go
git commit -m "feat(goal): 校验子代理完成判定与费用估算"
```

---

### Task 6: goal 进度与结果 SSE 推送

**Files:**
- Modify: `internal/backend/forwarder/goal.go`（加 `emitGoalProgress`、`emitGoalCompletion`、`truncateText`）
- Modify: `internal/backend/forwarder/goal_test.go`

**Interfaces:**
- Produces: `func (service *Service) emitGoalProgress(stream *ActiveStream, goal *GoalState) error`；`func (service *Service) emitGoalCompletion(stream *ActiveStream, goal *GoalState, kind string) error`；`func truncateText(text string, maxRunes int) string`。
- Consumes: `service.broker.Publish`（broker.go:361）、`buildSummaryStartedMessage` / `buildSummaryMessage` / `buildSummaryCompletedMessage`（events.go:77/90/105）、`buildTextDeltaMessage`（events.go:32）。推送顺序参考 `emitTurnSummary`（runtime_summary.go:218-229）。

- [ ] **Step 1: Write the failing test**

Append to `goal_test.go`:

```go
func TestTruncateText(t *testing.T) {
	if got := truncateText("你好世界", 2); got != "你好…" {
		t.Fatalf("truncateText = %q", got)
	}
	if got := truncateText("abc", 10); got != "abc" {
		t.Fatalf("truncateText short = %q", got)
	}
	if got := truncateText("", 5); got != "" {
		t.Fatalf("truncateText empty = %q", got)
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/backend/forwarder/ -run 'TestTruncateText' -v`
Expected: FAIL with "undefined: truncateText"

- [ ] **Step 3: Write minimal implementation**

Append to `internal/backend/forwarder/goal.go`:

```go
// truncateText 按 rune 截断文本并加省略号。
func truncateText(text string, maxRunes int) string {
	if maxRunes <= 0 {
		return ""
	}
	runes := []rune(strings.TrimSpace(text))
	if len(runes) <= maxRunes {
		return string(runes)
	}
	return string(runes[:maxRunes]) + "…"
}

// emitGoalProgress 向对话流注入一条 goal 进度摘要（summary 事件，Cursor 左侧摘要区可见）。
func (service *Service) emitGoalProgress(stream *ActiveStream, goal *GoalState) error {
	if service == nil || stream == nil || goal == nil {
		return nil
	}
	text := fmt.Sprintf("⏳ Goal 进度：pass %d | 工具调用 %d | 自检 %d 次%s", goal.ProviderPasses, goal.ToolCalls, goal.SelfChecks, progressSuffix(goal))
	return service.publishSummaryEvents(stream, text)
}

func progressSuffix(goal *GoalState) string {
	if strings.TrimSpace(goal.LastProgress) == "" {
		return ""
	}
	return " | 最近进展：" + goal.LastProgress
}

// emitGoalCompletion 在 goal 收口时向对话流注入最终汇报（可见 assistant 文本）。
func (service *Service) emitGoalCompletion(stream *ActiveStream, goal *GoalState, kind string) error {
	if service == nil || stream == nil || goal == nil {
		return nil
	}
	var text string
	switch kind {
	case "completed":
		text = fmt.Sprintf("✅ Goal 已完成：%s\n\n%s", goal.GoalText, firstNonEmpty(goal.CompletionText, "目标已达成。"))
	case "budget":
		text = fmt.Sprintf("⏹️ Goal 因预算上限停止：%s\n%s", goal.GoalText, firstNonEmpty(goal.StopReason, "预算超限。"))
	case "failed":
		text = fmt.Sprintf("❌ Goal 失败：%s\n%s", goal.GoalText, firstNonEmpty(goal.StopReason, "多次校验未通过。"))
	default:
		text = fmt.Sprintf("Goal 状态更新：%s", goal.StopReason)
	}
	return service.broker.Publish(stream.RequestID, StreamEvent{Message: buildTextDeltaMessage(text)})
}

// publishSummaryEvents 按 SummaryStarted → Summary → SummaryCompleted 顺序推送。
func (service *Service) publishSummaryEvents(stream *ActiveStream, text string) error {
	if err := service.broker.Publish(stream.RequestID, StreamEvent{Message: buildSummaryStartedMessage()}); err != nil {
		return err
	}
	if err := service.broker.Publish(stream.RequestID, StreamEvent{Message: buildSummaryMessage(text)}); err != nil {
		return err
	}
	return service.broker.Publish(stream.RequestID, StreamEvent{Message: buildSummaryCompletedMessage(stream.RequestID)})
}
```
（`firstNonEmpty` 在仓库已有实现（compiler.go:121 用过），直接复用包内同名函数；若不在 forwarder 包则在此定义。）

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/backend/forwarder/ -run 'TestTruncateText' -v`
Expected: PASS；`go build ./...` 通过。

- [ ] **Step 5: Commit**

```bash
git add internal/backend/forwarder/goal.go internal/backend/forwarder/goal_test.go
git commit -m "feat(goal): goal 进度与结果 SSE 推送"
```

---

### Task 7: provider 错误自动重试

**Files:**
- Modify: `internal/backend/forwarder/goal.go`（加 `handleGoalProviderError`、`goalErrorRetryReminder`）
- Modify: `internal/backend/forwarder/goal_test.go`
- Modify: `internal/backend/forwarder/actor.go:929`（handleProviderDoneEvent 错误路径，providerTerminalError 处理之前接线）

**Interfaces:**
- Produces: `func (service *Service) handleGoalProviderError(stream *ActiveStream, conversationID string, turnSeq int64, requestID, modelCallID string, providerPass int, err error) (bool, error)`。
- Consumes: Task 4 的 `appendGoalPromptContext`、`goalBudgetExceeded`；`providerTerminalError`（actor.go:929 用法）。

- [ ] **Step 1: Write the failing test**

Append to `goal_test.go`:

```go
func TestGoalErrorRetryReminder(t *testing.T) {
	reminder := goalErrorRetryReminder("upstream 429")
	if !strings.Contains(reminder, "429") {
		t.Fatalf("reminder missing error text: %q", reminder)
	}
	if !strings.Contains(reminder, "继续") {
		t.Fatalf("reminder missing retry instruction: %q", reminder)
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/backend/forwarder/ -run 'TestGoalErrorRetryReminder' -v`
Expected: FAIL with "undefined: goalErrorRetryReminder"

- [ ] **Step 3: Write minimal implementation**

Append to `internal/backend/forwarder/goal.go`:

```go
// goalErrorRetryReminder 是 provider 错误重试时注入的提示文案。
func goalErrorRetryReminder(errText string) PromptContextMessage {
	return newPromptContextReminder(promptContextSourceGoalErrorRetry,
		fmt.Sprintf("上一轮 provider 调用出错：%s\n请分析错误原因，换一种方式继续执行，直到完成目标。", truncateText(errText, 300)))
}

// handleGoalProviderError 在 goal 模式下把 provider 错误转为"注入错误摘要 + 续跑重试"，
// 超过 ErrorMaxRetries 才放行给 failStream。返回 (true, nil) 表示已重试（调用方 return）。
func (service *Service) handleGoalProviderError(stream *ActiveStream, conversationID string, turnSeq int64, requestID, modelCallID string, providerPass int, err error) (bool, error) {
	goal := stream.Goal
	if goal == nil {
		return false, nil
	}
	cfg := service.currentGoalConfig()
	if goal.ErrorRetries >= cfg.ErrorMaxRetries {
		return false, nil
	}
	goal.ErrorRetries++
	goal.ProviderPasses = providerPass
	goal.UpdatedAt = time.Now().UTC()
	appended, appendErr := service.appendGoalPromptContext(stream, conversationID, turnSeq, requestID, promptContextSourceGoalErrorRetry, goalErrorRetryReminder(errTextOf(err)).Message.Content)
	if appendErr != nil {
		return false, appendErr
	}
	if !appended {
		return false, nil // 已注入过同类提示，走 failStream 收口
	}
	if service.debug != nil {
		service.debug.LogRuntime(context.Background(), requestID, conversationID, "goal_error_retry", map[string]any{
			"provider_pass": providerPass,
			"retry_count":   goal.ErrorRetries,
			"error":         truncateText(errTextOf(err), 300),
		})
	}
	if err := service.syncSummaryCarryForward(conversationID, requestID, modelCallID); err != nil {
		return true, err
	}
	if err := service.publishCheckpoint(requestID, conversationID); err != nil {
		return true, err
	}
	return true, service.requestProviderAction(stream, providerActionResume)
}

// errTextOf 提取错误文本。
func errTextOf(err error) string {
	if err == nil {
		return ""
	}
	return err.Error()
}
```

Modify `internal/backend/forwarder/actor.go` `handleProviderDoneEvent` 错误路径：在 `:928-929`（`var providerErr providerTerminalError` 之前）插入：
```go
		// goal 模式：provider 错误先自动重试（有限次数），不要直接判失败。
		if stream.Goal != nil {
			retried, retryErr := service.handleGoalProviderError(stream, conversationID, turnSeq, requestID, modelCallID, providerPass, payload.Err)
			if retryErr != nil {
				return service.failStreamIfNonTerminal(stream, "unknown", retryErr)
			}
			if retried {
				return nil
			}
		}
```
（注意 `payload.Err != nil` 分支内、context-overflow / max-tokens 恢复（`:889-905`）之后插入，保持现有恢复优先级更高。`stream.Goal` 已在锁内读出，此处用局部变量或重新判空读取，实现者保证无数据竞争。）

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/backend/forwarder/ -run 'TestGoalErrorRetryReminder' -v`
Expected: PASS；`go build ./...` 通过。

- [ ] **Step 5: Commit**

```bash
git add internal/backend/forwarder/goal.go internal/backend/forwarder/goal_test.go internal/backend/forwarder/actor.go
git commit -m "feat(goal): provider 错误自动重试"
```

---

### Task 8: 导出 StartGoalStream / StopGoalStream + wails bindings

**Files:**
- Modify: `internal/backend/forwarder/service.go`（加导出方法 `StartGoalStream`、`StopGoalStream`）
- Modify: `internal/backend/forwarder/goal.go`（`StopGoalStream` 辅助：按 conversationID 找 stream）
- Modify: `internal/backend/host.go`（转发 `GoalSnapshots` / `StartGoal` / `StopGoal`）
- Create: `internal/client/goal.go`（wails bindings `GetGoals` / `StartGoal` / `StopGoal`）
- Create: `internal/client/goal_test.go`
- Modify: `frontend/src/services/browserBindings.js`（mock 三个方法）
- Modify: `frontend/src/services/clientApi.js`（封装三个调用）

**Interfaces:**
- Produces: `func (service *Service) StartGoalStream(goalText, modelID string) (string, error)`（返回 conversationID）；`func (service *Service) StopGoalStream(conversationID string) error`；host 层 `func (host *Host) StartGoal(goalText, modelID string) (string, error)` / `StopGoal(conversationID string) error` / `GoalSnapshots() []forwarder.GoalSnapshot`；binding 层 `func (service *ProxyService) GetGoals() ([]forwarder.GoalSnapshot, error)` / `StartGoal(goalText, modelID string) (string, error)` / `StopGoal(conversationID string) error`。
- Consumes: `InboundIntent`（Task 2 已加 GoalMode/GoalText）、`handleRunIntent`（service.go:709）、`handleCancelIntent`（service.go:1036）、broker 找流（`service.broker`）。

- [ ] **Step 1: Write the failing test**

Create `internal/client/goal_test.go`（仿 `internal/client/provider_balance_configured_test.go` 的构造模式）：

```go
package client

import (
	"testing"
)

func TestGoalBindingsEmpty(t *testing.T) {
	// 复用项目内最小 ProxyService 构造（参照 provider_balance_configured_test.go:123-133 的 newProviderBalanceTestService）。
	service := newGoalTestService(t)
	goals, err := service.GetGoals()
	if err != nil {
		t.Fatalf("GetGoals error: %v", err)
	}
	if goals == nil {
		t.Fatal("GetGoals must return non-nil slice")
	}
}
```
（`newGoalTestService` 为测试辅助，按现有测试构造模式实现：`serverconfig.NewStore` + `t.TempDir()` + host 装配；若现有辅助可复用则直接复用。）

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/client/ -run 'TestGoalBindingsEmpty' -v`
Expected: FAIL with "undefined: GetGoals / newGoalTestService"

- [ ] **Step 3: Write minimal implementation**

Modify `internal/backend/forwarder/service.go`（新增导出方法，可放 goal.go 保持聚合）：

```go
// StartGoalStream 以 goal 模式启动一个新会话（前端 Goal 面板入口）。
// 复用 handleRunIntent 路径：构造最小 run intent 并标记 GoalMode。
// 返回 conversationID；modelID 为空时报错（前端必须选择模型）。
func (service *Service) StartGoalStream(goalText, modelID string) (string, error) {
	if service == nil {
		return "", fmt.Errorf("service unavailable")
	}
	if strings.TrimSpace(goalText) == "" {
		return "", fmt.Errorf("goal text is required")
	}
	if strings.TrimSpace(modelID) == "" {
		return "", fmt.Errorf("model id is required")
	}
	conversationID := uuid.NewString()
	intent := InboundIntent{
		RequestID:      uuid.NewString(),
		ConversationID: conversationID,
		UserMessage:    &agentv1.UserMessage{Text: strings.TrimSpace(goalText)},
		ModelID:        strings.TrimSpace(modelID),
		ModelName:      strings.TrimSpace(modelID),
		Mode:           agentv1.AgentMode_AGENT_MODE_AGENT,
		GoalMode:       true,
		GoalText:       strings.TrimSpace(goalText),
	}
	if err := service.handleRunIntent(intent); err != nil {
		return "", err
	}
	return conversationID, nil
}

// StopGoalStream 停止指定会话的 goal 执行（复用取消路径）。
func (service *Service) StopGoalStream(conversationID string) error {
	if service == nil || strings.TrimSpace(conversationID) == "" {
		return fmt.Errorf("conversation id is required")
	}
	// broker 无按 conversationID 的查流方法：用 OtherConversationRequestIDs（broker.go:199）
	// 拿该 conversation 的活动 requestID，再 Get 流。
	var stream *ActiveStream
	for _, requestID := range service.broker.OtherConversationRequestIDs(conversationID, "") {
		if candidate, ok := service.broker.Get(requestID); ok {
			stream = candidate
			break
		}
	}
	if stream == nil {
		// 无活动流：把内存里的 goal 状态标记为 stopped。
		service.goalsMu.Lock()
		if goal, ok := service.goals[conversationID]; ok && goal.Status == GoalStatusRunning {
			goal.Status = GoalStatusStopped
			goal.StopReason = "用户手动停止"
			goal.UpdatedAt = time.Now().UTC()
		}
		service.goalsMu.Unlock()
		return nil
	}
	return service.handleCancelIntent(InboundIntent{
		RequestID:      stream.RequestID,
		ConversationID: conversationID,
	})
}
```
（`broker.ActiveStreamByConversation` 若不存在，用 `service.broker` 现有按 requestID 查询的 API 或遍历；实现者以 broker.go 实际 API 为准，必要时新增该方法。）
（若 `handleCancelIntent` 要求 intent 里有 cancel 标记而非仅 RequestID/ConversationID，实现者按 service.go:1036-1143 实际字段补全。）

Modify `internal/backend/host.go`：仿 `DelegationTaskSnapshots`（`:76-88`）与 `CancelDelegationTask`（`:108-115`）加：
```go
// GoalSnapshots 返回当前 forwarder 的 goal 状态快照。
func (host *Host) GoalSnapshots() []forwarder.GoalSnapshot {
	module := host.agentModule
	if module == nil || module.Service == nil {
		return nil
	}
	return module.Service.GoalSnapshots()
}

// StartGoal 以 goal 模式启动新会话，返回 conversationID。
func (host *Host) StartGoal(goalText, modelID string) (string, error) {
	module := host.agentModule
	if module == nil || module.Service == nil {
		return "", fmt.Errorf("forwarder unavailable")
	}
	return module.Service.StartGoalStream(goalText, modelID)
}

// StopGoal 停止指定会话的 goal 执行。
func (host *Host) StopGoal(conversationID string) error {
	module := host.agentModule
	if module == nil || module.Service == nil {
		return fmt.Errorf("forwarder unavailable")
	}
	return module.Service.StopGoalStream(conversationID)
}
```

Create `internal/client/goal.go`（仿 `internal/client/delegation.go` 的 binding 模式）：

```go
package client

import (
	"cursor/internal/backend/forwarder"
)

// GetGoals 返回全部 goal 状态快照。
func (service *ProxyService) GetGoals() ([]forwarder.GoalSnapshot, error) {
	if service == nil || service.backendHost == nil {
		return nil, nil
	}
	return service.backendHost.GoalSnapshots(), nil
}

// StartGoal 以 goal 模式启动新会话。
func (service *ProxyService) StartGoal(goalText, modelID string) (string, error) {
	if service == nil || service.backendHost == nil {
		return "", nil
	}
	return service.backendHost.StartGoal(goalText, modelID)
}

// StopGoal 停止指定会话的 goal。
func (service *ProxyService) StopGoal(conversationID string) error {
	if service == nil || service.backendHost == nil {
		return nil
	}
	return service.backendHost.StopGoal(conversationID)
}
```
（`ProxyService.backendHost` 字段名以 `internal/client/service.go:34-82` 实际为准；实现者对照修正。）

Modify `frontend/src/services/clientApi.js`：仿 `getDelegationConfig`（`clientApi.js:352-356`）加：
```js
export async function getGoals() {
  return desktopOrMock(null, '@bindings/cursor/internal/client/proxyservice', 'GetGoals', []);
}
export async function startGoal(goalText, modelID) {
  return desktopOrMock(null, '@bindings/cursor/internal/client/proxyservice', 'StartGoal', [goalText, modelID]);
}
export async function stopGoal(conversationID) {
  return desktopOrMock(null, '@bindings/cursor/internal/client/proxyservice', 'StopGoal', [conversationID]);
}
```
（binding 模块路径以现有生成的 `frontend/bindings/cursor/internal/client/` 实际文件为准；新增 Go binding 后重跑 wails3 构建生成。browser-preview 在 `browserBindings.js` 补对应 mock。）

Modify `frontend/src/services/browserBindings.js`：加
```js
GetGoals: async () => [],
StartGoal: async (_goalText, _modelID) => ({ conversationId: 'mock-' + Date.now() }),
StopGoal: async (_conversationID) => null,
```

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/client/ -run 'TestGoalBindingsEmpty' -v`
Expected: PASS；`go build ./...` 通过。重跑 wails3 构建生成 bindings（`Taskfile.yml` 中 wails build 任务或 `wails3 generate bindings`），确认 `frontend/bindings/` 出现 GetGoals/StartGoal/StopGoal。

- [ ] **Step 5: Commit**

```bash
git add internal/backend/forwarder/goal.go internal/backend/host.go internal/client/goal.go internal/client/goal_test.go frontend/src/services/browserBindings.js frontend/src/services/clientApi.js frontend/bindings/
git commit -m "feat(goal): StartGoal/StopGoal/GetGoals bindings"
```

---

### Task 9: 前端 Goal 面板与设置页

**Files:**
- Create: `frontend/src/views/Goal.vue`
- Modify: `frontend/src/router/index.js`（路由）
- Modify: `frontend/src/views/Home.vue`（顶栏 ActionMenu 入口 + `handleOpenGoal`）
- Create: `frontend/src/components/settings/categories/GoalSettings.vue`
- Modify: `frontend/src/components/settings/settingsCategories.js`（注册分类）
- Modify: `frontend/src/views/Settings.vue`（`categoryComponents`）
- Modify: `frontend/src/utils/configNormalize.js`、`frontend/src/state/appState.js`（goal 配置字段 + save action）
- Modify: `frontend/src/i18n/`（跑 `npm --prefix frontend run i18n:scan` 自动生成，不手写 key）

**Interfaces:**
- Consumes: `getGoals` / `startGoal` / `stopGoal`（Task 8）；`loadUserConfig` / `saveUserConfig`（clientApi.js:95-101）；`appState`（appState.js:503）。
- Produces: Goal.vue（goal 列表卡片 + 发起表单 + 停止按钮）；GoalSettings.vue（`autosave.run("goal.xxx", ...)` 保存链路，参照 AdvancedSettings.vue:194-195）。

- [ ] **Step 1: Write the failing view**

创建 `frontend/src/views/Goal.vue`（骨架，参照 Settings.vue 的 `Card`/flex 布局与 `components/ui/*`）：

```vue
<template>
  <div class="goal-page">
    <div class="goal-header">
      <h1>{{ localized('目标执行（Goal）', 'Goal') }}</h1>
      <p class="goal-subtitle">{{ localized('让 Agent 带目标循环执行，直到任务真正完成', 'Goal loop') }}</p>
    </div>

    <Card class="goal-form-card">
      <h2>{{ localized('发起 Goal', 'Start Goal') }}</h2>
      <textarea
        v-model="goalText"
        class="goal-input"
        :placeholder="localized('输入目标，例如：跑通全部测试并修复失败用例', 'Describe the goal')"
        rows="3"
      />
      <div class="goal-form-actions">
        <Select v-model="modelID" :options="modelOptions" class="goal-model-select" />
        <Button :disabled="!canStart" @click="handleStart">{{ localized('开始执行', 'Start') }}</Button>
      </div>
    </Card>

    <div class="goal-list">
      <Card v-for="goal in goals" :key="goal.conversationId" class="goal-card">
        <div class="goal-card-head">
          <span class="goal-status" :class="goal.status">{{ statusLabel(goal.status) }}</span>
          <span class="goal-time">{{ goal.startedAt }}</span>
        </div>
        <p class="goal-text">{{ goal.goalText }}</p>
        <p class="goal-meta">
          {{ localized('pass 数', 'passes') }}: {{ goal.providerPasses }} ·
          {{ localized('工具调用', 'tool calls') }}: {{ goal.toolCalls }} ·
          {{ localized('自检', 'self checks') }}: {{ goal.selfChecks }} ·
          {{ localized('费用估算', 'cost') }}: ${{ goal.costEstimateUsd?.toFixed(4) ?? '0' }}
        </p>
        <p v-if="goal.lastProgress" class="goal-progress">{{ goal.lastProgress }}</p>
        <p v-if="goal.completionText" class="goal-completion">{{ goal.completionText }}</p>
        <p v-if="goal.stopReason" class="goal-stop-reason">{{ goal.stopReason }}</p>
        <Button v-if="goal.status === 'running'" size="small" @click="handleStop(goal.conversationId)">
          {{ localized('停止', 'Stop') }}
        </Button>
      </Card>
      <p v-if="goals.length === 0" class="goal-empty">{{ localized('暂无 goal 记录', 'No goals yet') }}</p>
    </div>
  </div>
</template>
```
（script 部分：`localized` 从 `@/i18n/runtime` 导入（runtime.js:176）；`goals` 轮询 `getGoals()`（3s 间隔）；`handleStart` 调 `startGoal(goalText, modelID)` 后清空输入并刷新；`handleStop` 调 `stopGoal(conversationId)`；`modelOptions` 从 `appState` 可用模型生成。组件用 `components/ui/{Card,Button,Select}.vue` 现有 API，以实际导出为准。）

- [ ] **Step 2: Verify the view fails to build**

Run: `npm --prefix frontend run build`
Expected: 失败（路由未注册、clientApi 导入缺失）——这证明新视图尚未接通。

- [ ] **Step 3: Wire routing, entry and settings**

Modify `frontend/src/router/index.js`：import Goal（仿 :12）+ 路由（仿 :64-68）：
```js
{ path: '/goal', component: Goal, meta: { showIcon: false, title: '目标', directlyClose: true } },
```
Modify `frontend/src/views/Home.vue`：顶栏 `ActionMenu`（:242 内）加菜单项，script 加（仿 :80-82 双模式）：
```js
function handleOpenGoal() {
  if (isBrowserPreview) return router.push('/goal');
  return openGoalWindow();
}
```
（`openGoalWindow` 若桌面独立窗口需要新 wails binding；MVP 可仅浏览器预览路由 + 桌面直接内嵌页面，实现者按 Home.vue 现有 `openModelConfigWindow` 模式对齐。）

Create `frontend/src/components/settings/categories/GoalSettings.vue`：
```vue
<template>
  <SettingsSection :title="localized('Goal 循环执行', 'Goal Loop')" :description="localized('配置 goal 模式的预算与完成判定', 'Goal budget & verification')">
    <SettingsRow :label="localized('启用前端面板发起', 'Enable panel')">
      <Switch :model-value="enabled" @update:model-value="setEnabled" />
    </SettingsRow>
    <SettingsRow :label="localized('最大 provider pass 数', 'Max provider passes')">
      <Input type="number" :model-value="maxPasses" @update:model-value="setMaxPasses" />
    </SettingsRow>
    <SettingsRow :label="localized('最大时长（秒，0=不限）', 'Max duration seconds')">
      <Input type="number" :model-value="maxDuration" @update:model-value="setMaxDuration" />
    </SettingsRow>
    <SettingsRow :label="localized('费用上限（USD，0=不限）', 'Max cost USD')">
      <Input type="number" :model-value="maxCost" @update:model-value="setMaxCost" />
    </SettingsRow>
    <SettingsRow :label="localized('完成自检轮数', 'Self-check passes')">
      <Input type="number" :model-value="selfCheckPasses" @update:model-value="setSelfCheckPasses" />
    </SettingsRow>
  </SettingsSection>
</template>
```
（script 用 `useSettingsAutosave`（composables/useSettingsAutosave.js）+ `props.autosave.run("goal.enabled", async () => saveUserConfig(payload))`，参照 AdvancedSettings.vue:194-195；`appState` 加 `goal` 配置字段与 save action，`configNormalize.js` 加 `normalizeGoal`。）

Modify `frontend/src/components/settings/settingsCategories.js`：`SETTINGS_CATEGORIES`（:27-76）加 goal 分类；Modify `frontend/src/views/Settings.vue` `categoryComponents`（:51-60）加 `GoalSettings`。

- [ ] **Step 4: Verify build + i18n scan**

Run: `npm --prefix frontend run i18n:scan && npm --prefix frontend run build`
Expected: 构建通过；`locales/{zh-CN,en-US,ja-JP,ru-RU}.json` 与 `generated/catalog.json` 自动补全新 key。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/views/Goal.vue frontend/src/router/index.js frontend/src/views/Home.vue frontend/src/components/settings/categories/GoalSettings.vue frontend/src/components/settings/settingsCategories.js frontend/src/views/Settings.vue frontend/src/utils/configNormalize.js frontend/src/state/appState.js frontend/src/i18n/
git commit -m "feat(goal): 前端 Goal 面板与设置页"
```

---

### Task 10: 官方 Cursor 组合方案模板与文档

**Files:**
- Create: `docs/goal-workflow/README.md`
- Create: `docs/goal-workflow/.cursor/rules/goal.mdc`
- Create: `docs/goal-workflow/commands/goal.md`

**Interfaces:** 无代码接口；交付物为模板与说明，供不改官方 Cursor 时近似实现 goal 行为。

- [ ] **Step 1: Write the rules template**

Create `docs/goal-workflow/.cursor/rules/goal.mdc`：

```markdown
---
description: GOAL 循环执行规则：当用户以 /goal 发起任务时，Agent 必须循环执行直到目标真正完成，失败自动重试，定期汇报进度。
globs: []
alwaysApply: false
---

# GOAL 循环执行

当用户以 `/goal <目标>` 发起任务时，按以下规则执行：

1. **循环执行**：一轮结束（没有工具调用）不代表完成。自检目标是否达成：未达成则继续执行下一轮，直到目标真正达成。
2. **失败重试**：工具执行失败时分析原因、换一种方式重试，不要直接放弃。
3. **自主决策**：持续调用工具推进目标，不要轻易停下来问用户；只有真正无法继续时才提问。
4. **进度汇报**：每完成一个阶段，输出一行简短进度（形如 `⏳ 进度：...`）。
5. **完成标准**：目标所有要求满足后才算完成。完成时先单独输出一行 `[goal:complete]`，再输出最终完成报告（做了什么、验证了什么、结果如何）。不要在没有真正完成时输出该标记。
6. **预算自控**：目标较大时主动拆阶段，避免单次任务无限膨胀；每 10 轮工具调用做一次自我总结。
```

- [ ] **Step 2: Write the command template**

Create `docs/goal-workflow/commands/goal.md`：

```markdown
---
description: 以 goal 模式执行任务：循环执行、失败重试直到完成
---

你当前处于 GOAL 模式。你的目标：{{input}}

请遵循 .cursor/rules/goal.mdc 的 GOAL 循环执行规则，循环执行直到目标真正达成。
```

- [ ] **Step 3: Write the README**

Create `docs/goal-workflow/README.md`，说明：
- 官方 Cursor 近似方案：把 `rules/goal.mdc` 放入项目 `.cursor/rules/`、`commands/goal.md` 放入 `.cursor/commands/`（或做成 skill），用 `/goal 目标` 发起；Agent 会按规则循环执行、失败重试、汇报进度。限制：无真实预算上限、无校验子代理二次确认、进度仅靠 Agent 自觉汇报。
- 与 cursor-byok 内置 goal 的差异对比表（预算 / 校验 / 状态面板 / 停止控制）。
- cursor-byok goal 使用说明：`/goal 目标` 或前端 Goal 面板；配置入口 `设置 → Goal`；状态查看：对话流 summary + 前端 Goal 面板。

- [ ] **Step 4: Verify docs render**

Run: 人工检查三份文件内容完整、路径引用正确。
Expected: README 中的路径与实际模板文件一致。

- [ ] **Step 5: Commit**

```bash
git add docs/goal-workflow/
git commit -m "docs(goal): 官方 Cursor 组合方案模板与使用说明"
```

---

## Self-Review

**1. Spec coverage（对照用户需求）：**
- 失败/出错自动重试直到完成 → Task 7（provider 错误）+ Task 4（工具失败靠 goal 指令 3 号要求 + 自检续跑）
- 后台持续运行 → 前台持续执行（用户选定）：同一会话循环，进度 SSE 注入 → Task 4/6；前端面板可同时查看/停止 → Task 8/9
- 自主决策少问用户 → goal 指令 1 号要求（Task 3）+ Ask 工具保留但提示优先自决（未强制拦截，避免改动工具白名单影响面；文档注明可选后续）
- 明确完成判定与汇报 → Task 5（校验子代理）+ Task 6（完成汇报推送）
- 在 Cursor 客户端显示 goal → Task 6（summary/text_delta 注入对话流）
- 前端 Goal 面板双入口 → Task 8（StartGoal binding）+ Task 9（Goal.vue）
- 可配置预算上限 → Task 1（配置）+ Task 4（passes/时长/费用检查）+ Task 9（设置页）
- 官方 Cursor 组合方案 → Task 10

**2. Placeholder scan：** 无 TBD/TODO；各任务均含可执行测试与实现代码。两处"实现者按实际 API 修正"（broker 查流、binding 模块路径）是有明确参照的适配指令，非占位。

**3. Type consistency：** `GoalState`/`GoalRuntimeConfig`/`GoalSnapshot`/`goalConfigProvider`/`handleGoalPassFinished`/`goalVerifyCompletion`/`emitGoalProgress`/`emitGoalCompletion`/`StartGoalStream`/`StopGoalStream` 在定义与消费任务间签名一致；`goalCompletionMarker`/`promptContextSourceGoalIdle`/`promptContextSourceGoalVerifyFeedback`/`promptContextSourceGoalErrorRetry` 等常量在 Task 4 定义、Task 7 复用；辅助函数已核实：`firstNonEmpty`（broker.go:496）、`newPromptContextReminder`（reminders.go:165）、`broker.Get`/`OtherConversationRequestIDs`（broker.go:130/199）均存在；`maxInt` 用 Go 内置 `max`；`truncateText` 由 Task 6 定义。已修正：`goalIdleReminder`/`goalErrorRetryReminder` 改用 `newPromptContextReminder`，`StopGoalStream` 用 `OtherConversationRequestIDs`+`Get` 查流。

**已知取舍（文档注明）：** Ask 工具未强制拦截（MVP）；goal 状态仅内存；费用估算依赖 pricing provider；前端桌面独立窗口可后续加 binding。

**Reasonix 借鉴记录：** 本计划调研了 Reasonix 的 Goal 模式（docs/GUIDE.md::s022、docs/GOAL_ENFORCEMENT.zh-CN.md，实现于 internal/control/controller.go 的 Goal 状态机 / advanceGoalAfterTurn）。吸收：① `[goal:complete]` 显式完成声明拦截（Task 3/4，替代"连续无工具调用即判定完成"）；② idle 停顿提醒并随轮数升级（Task 4 goalIdleReminder）；③ `/goal --strict` 不允许覆盖/兜底（Task 1/2/5）；④ 校验连续未通过 → 结构性换策略（Task 4 StaleCount，对应 Reasonix AutoResearch 的 stale pivot）；⑤ 完成声明 + 步骤拆解进 goal 指令（Task 3）。未移植（记录理由）：todo 状态机（cursor-byok 无独立 todo 工具，用"步骤拆解 + 校验子代理证据审计"替代，中远期可在 GoalSnapshot 展示步骤）；parallel_tasks 并行子任务（cursor-byok 已有 multitask 委派体系）；AutoResearch 持久化状态目录（goal 状态 MVP 仅内存，中远期可加 .goal/ 状态文件）。
