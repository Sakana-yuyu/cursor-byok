---
name: image-see
description: Read local or remote images through the bundled vision-reader MCP server when image description or OCR is needed.
---

# Image See

Use the `vision-reader` MCP server to inspect an image and return a grounded visual description and visible text.

The server accepts a local image path or an HTTP/HTTPS image URL. Include a focused question when the task needs details beyond the default description and OCR.
