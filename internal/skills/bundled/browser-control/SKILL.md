---
name: browser-control
description: 需要打开、操作、检查或验证浏览器页面和本地 Web 应用时使用；覆盖导航、点击、输入、截图和交互状态检查。
---

# Browser Control

进行浏览器控制时，先确认目标页面和当前状态，再执行最小必要操作。

- 每个点击、输入和导航都要检查结果是否生效。
- 记录页面错误、网络失败、不可点击元素和布局溢出。
- 对关键流程至少验证初始、处理中、成功和失败状态。
- 只在用户授权范围内操作页面，不绕过权限或安全提示。
