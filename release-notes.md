## v0.0.62

### 新功能
- **首页一键启动 Cursor**：自动检测 Cursor 安装路径，检测到时在首页显示「启动 Cursor」按钮，无需手动定位可执行文件。
- **统计浮窗边缘吸附与位置记忆**：浮窗拖近屏幕边缘时自动收缩为小条不遮挡内容，鼠标悬停时展开；窗口位置会持久化保存，下次打开恢复到上次位置，并支持开机按偏好自动打开。
- **供应商分组按名称显示**：模型分组视图新增按供应商名称展示，未命名时回退为「默认分组」。

### 修复
- **OpenAI Responses 工具名称非法导致请求失败**：部分工具名称含特殊字符时会被上游拒绝。现对工具名称做规范化（仅保留 `a-zA-Z0-9_-`，非法字符替换为下划线），保证兼容性。
- **上游错误信息难以阅读**：上游返回的 JSON 错误体现在会提取关键信息并转换为友好提示（如「上游服务器返回 400 错误」），便于快速定位问题。
- **请求明细分页区间显示不完整**：分页标签从「起始 / 总数」改为「起始-结束 / 总数」，更清晰地展示当前页范围。
- **批量编辑供应商保存无反馈**：保存成功后会弹出确认提示并显示已更新的模型数量。

### 优化
- **本地响应缓存默认启用并提升命中率**：缓存配置默认开启；缓存 key 不再包含每次变化的动态字段（请求估算 token、编译摘要），大幅提升重复请求的缓存命中率；同时新增缓存 key 计算的二级缓存，避免对相同请求重复进行 JSON 序列化与哈希。
- **用量写入防抖批量落盘**：用量事件改为 2 秒防抖 / 50 条批量的合并写入，减少磁盘 I/O 开销。
- **统计卡片自动刷新**：首页统计卡片每 5 秒自动刷新，无需手动操作。
- **流式空闲心跳优化**：Anthropic/OpenAI 适配器在收到有效内容（文本/思考增量）时标记活跃，避免误判空闲超时。

> **Windows 用户注意**：安装时若被 SmartScreen 拦截，点击「更多信息」->「仍要运行」即可。
