## v0.0.74

### 新功能
- **统计清除同步**：清除会话统计时，站点消耗同步归零并立即刷新，两张首页卡片实时保持一致。
- **Cursor 静默启动**：Windows 下启动 Cursor 时隐藏控制台窗口，不再出现黑色终端窗口，正常 GUI 窗口不受影响。

### 修复
- **清除后数据回写**：修复清除统计后运行中的写入队列可能延迟回写旧事件、导致数据恢复的问题，清除边界在磁盘与内存间保持同步。

### 优化
- **平台隔离**：Windows 静默启动逻辑独立成文件，非 Windows 构建不受影响。

> **Windows 用户注意**：安装时若被 SmartScreen 拦截，点击「更多信息」->「仍要运行」即可。