# 更新日志

---

## [v0.0.65] — 2026-07-30

### ✨ 新功能

- **Three.js 动态全息浮窗**：统计浮窗新增基于 Three.js 的三种 3D 可视化布局——Card（3D 数据柱 + 动态网格 + 直线扫描）、Engine（环形仪表盘 + 浮动节点 + 旋转扫描线）、Orb（中心发光球体 + 轨道卫星 + 粒子流）。内置 FPS 监控自动降级、3 级质量预设、收缩态粒子优化、移动端自动低质量模式
- **更新弹窗 Markdown 渲染**：版本更新说明现在正确渲染为富文本（标题/列表/加粗/代码/引用块），不再显示 markdown 原文

### 🐛 修复

- **macOS DMG 构建偶发失败**：修复 `hdiutil: create failed - Resource busy` 错误——创建 DMG 前先卸载可能残留的旧挂载点，create-dmg 失败时自动回退到 hdiutil
- **识图请求误触发图像生成**：发送图片识图时，若消息含"图片"等关键词，会错误注入 `image_generation` 工具，导致无图像生成权限的账户报 403 `Image generation is not enabled for this group`。现已修复：有图片附件 → 直接跳过注入；关键词匹配改为「生成动词 + 图像名词」双重条件
- **悬浮窗每次打开回到屏幕中心**：首次位置轮询会覆盖已存坐标，现已用已存坐标初始化追踪变量防止误覆盖

### ⚡ 优化

- **悬浮窗透明边框**：卡片面板边框由不透明深灰改为近乎透明的细边 + 毛玻璃模糊，消除明显黑边
- **悬浮窗贴标识别**：轮询间隔 1000ms → 300ms；进入吸附阈值 20px → 40px；增加 80px 迟滞防边界抖动；修正多屏坐标偏移
- **上下文维护**：LLM 摘要压缩前先持久化截断陈旧超大工具结果，延长缓存命中、减少摘要 API 调用

> **Windows 用户注意**：安装时若被 SmartScreen 拦截，点击「更多信息」->「仍要运行」即可。

---

## [v0.0.64] — 2026-07-30

### 🐛 修复

- **识图请求误触发图像生成**：发送图片识图时，若消息含"图片"等关键词，会错误注入 `image_generation` 工具，导致无图像生成权限的账户报 403 `Image generation is not enabled for this group`。现已修复：有图片附件 → 直接跳过注入；关键词匹配改为「生成动词 + 图像名词」双重条件，避免「分析这张图片」等识图句式误触发
- **悬浮窗每次打开回到屏幕中心**：首次位置轮询会把窗口初始位置（屏幕中心）覆盖掉已存坐标。现在 `onMounted` 时用已存坐标初始化内部追踪变量，防止误覆盖

### ✨ 新功能

- **悬浮窗贴边收缩开关**：悬浮窗 header 右侧新增小按钮，可随时开启/关闭「贴边自动收缩」功能，设置自动持久化

### ⚡ 优化

- **悬浮窗贴标识别**：轮询间隔 1000ms → 300ms；进入吸附阈值 20px → 40px；增加 80px 离开迟滞阈值防边界抖动；修正多屏环境下 `availLeft/availTop` 偏移导致的边缘误判
- **上下文维护**：在触发昂贵的 LLM 摘要压缩前，先持久化截断陈旧超大工具结果（snip/prune），尽可能延长缓存命中、减少摘要 API 调用

> **Windows 用户注意**：安装时若被 SmartScreen 拦截，点击「更多信息」->「仍要运行」即可。

---

## [v0.0.63] — 2026-07-30

### ✨ 新功能

- **模型协议诊断与一键优化**：新增模型协议诊断功能，自动检测已导入模型中 Claude/Gemini 被误配为 OpenAI 协议的问题（这是缓存失效的根因）。在模型配置页点击「一键诊断优化」即可扫描并一键修正为原生协议（Anthropic messages / Gemini native），启用前缀缓存。
- **独立诊断页面**：新增 `/diagnostics` 页面，可查看完整问题列表并选择性修正。

### 🐛 修复

- **Claude 渠道缓存失效根因修复**：type=openai 的渠道承载 Claude 模型时，运行时自动升级到 Anthropic 原生协议（cache_control 断点），使前缀缓存生效。升级失败时自动降级回 OpenAI 协议，并通过 `protocolMode=fixed` 提供逃生口。
- **供应商详情页导入分组丢失**：从供应商详情页批量导入模型时，OpenAI 类型模型的 `protocolGroup`/`openAIRequestGroup` 被错误置空，导致分组丢失。现与模型目录导入逻辑对齐，按模型名与地址正确推断分组。
- **usage 缺失 cache_creation_tokens**：OpenAI 协议路径现在正确解析 Claude 中转返回的 `cache_creation_tokens` 字段，缓存写入统计完整。

### ⚡ 优化

- **重试策略精简，减少等待时间**：外层跨渠道重试上限调整为 10 次；内层连接重试不再对 429/5xx 自行退避重试（避免内外双层退避叠加放大等待时间），总尝试次数精确为 10 次，耗尽后直接抛出错误。
- **prompt_cache_key 默认开启**：对所有 OpenAI 兼容 provider 默认发送 `prompt_cache_key`（仅对已知会报错的 provider 关闭），提升第三方中转的前缀缓存命中率。
- **设置抽屉布局优化**：设置抽屉内的提示词与本地化区域改为卡片式布局，内容溢出时可正确滚动与截断，长内容不再撑破面板。
- **完整多语言支持**：补全所有界面文案的英语、日语、俄语翻译（共 559 条），消除空翻译。
- **统计浮窗样式优化**：浮窗布局与间距调整，信息展示更紧凑。

> **Windows 用户注意**：安装时若被 SmartScreen 拦截，点击「更多信息」->「仍要运行」即可。
