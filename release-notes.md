## v0.0.58

### 新功能
- **内置官方价格 + 三级价格优先级**：模型花费估算现按「手动配价 > 中转站探测价 > 内置官方价」三级优先级取价。内置目录已收录 Claude/GPT/Gemini/Grok/Qwen/DeepSeek/Kimi/GLM/MiMo/MiniMax/StepFun 各族官方公布价格；即使从未手动配价或探测失败，首页与浮窗的「价值估算」也能显示金额而非「-」。内置价仅运行时注入，不写入 config.yaml。
- **StatsOverlay 实时数据条**：卡片式与引擎仪表式浮窗底部新增极简数据条（命中率·Token·轮次 + 刷新跳动指示），球形样式不显示。浮窗窗口高度已相应加高以容纳底部条。

### 优化
- **ModelEditor 价格来源标记**：价格编辑面板标注当前生效价格来源（手动配价 / 中转站探测价 / 未配置将使用内置官方价），便于判断估算依据。
- **HomeMetricsCard 价值估算 tooltip**：标注价格优先级，说明金额为区间内已知价格模型的美元花费合计。
- **StatsOverlay 全文案国际化**：浮窗所有文案完成国际化并补全中/英/日/俄多语言翻译。

### 修复
- **请求明细重复**：`turn_finalized` 事件不再计入请求明细列表与总数，与 `provider_call` 口径一致，避免同一轮出现两条明细及分页错乱。
- **分组模式切换后列表滤空**：切换供应商分组模式时清掉旧模式遗留的 group key 聚焦并回退全量列表，避免名称/分组消失。
- **连接分组丢失首条默认分组名**：连接分组的展示名保持为首条 adapter 的分组名（稳定、可路由），多名称聚合改由展示层现算，不再因「首条非空」覆盖丢弃「默认分组」信息。

> **Windows 用户注意**：安装时若被 SmartScreen 拦截，点击「更多信息」->「仍要运行」即可。
