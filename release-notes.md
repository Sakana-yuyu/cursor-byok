## v0.0.73

### 新功能
- **监督式委派**：支持由高级模型担任顾问与监工，持续检查子代理进度，并执行纠偏、重试、改派、升级或熔断。
- **监督策略设置**：可单独选择监督模型、复核模型和执行模型组，并配置纠偏、重试、轮次及不可用处理策略。

### 修复
- **重复复核与循环**：修复 ShellStream 心跳造成监督决策重复执行或长期无法收口的问题。
- **异步结果隔离**：加强 parent request、exec ID、message ID 和监督轮次校验，避免晚到结果污染其他任务。
- **范围检测**：修复 Windows 根相对、盘符相对及工作区外路径可能绕过委派 scope 检查的问题。
- **取消与保留策略**：修复取消竞态、终态快照覆盖及历史任务保留数量持续增长的问题。

### 优化
- **非阻塞执行**：Supervisor、Cursor worker 和本地 worker 通过异步事件协调，单个子任务失败不会阻塞其他任务或主 Agent。
- **循环识别**：根据有效进展、重复工具、修改文件、完成证据和错误状态识别偏离与无进展任务。
- **设置可靠性**：监督设置支持独立保存、失败回滚、重试、刷新恢复和完整国际化。

> **Windows 用户注意**：安装时若被 SmartScreen 拦截，点击「更多信息」->「仍要运行」即可。
