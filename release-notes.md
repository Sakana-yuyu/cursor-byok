## v0.0.55

### 修复
- **非视觉模型图片请求兼容**：对明确不支持图片输入的模型自动将图片内容替换为文字占位，避免 Kimi 等模型因 `image_url` 请求失败。
- **工具 Schema 兼容性增强**：修复 OpenAI/Anthropic 工具参数中的 `required:null` 或缺失 `required`，避免严格校验网关返回 400。
- **Kimi 偶发错误重试更稳健**：将特定的临时 400 错误判定为可重试，并过滤 reasoning-only 的残缺 assistant 回合。

### 新功能
- **供应商级配置批量编辑**：在供应商详情页支持同步更新同一供应商下所有模型的共享配置，减少重复维护。
- **模型上下文能力识别扩展**：补充更多模型的上下文窗口、最大输出与视觉能力信息，供前后端统一使用。

### 优化
- **转发请求参数更完整**：供应商转发路径支持传递模型能力与额外兼容参数，提升不同协议和中转网关的适配稳定性。
- **模型编辑体验优化**：前端按模型能力展示上下文信息，并在编辑供应商配置时提供更明确的冲突处理。

> **Windows 用户注意**：安装时若被 SmartScreen 拦截，点击「更多信息」->「仍要运行」即可。
