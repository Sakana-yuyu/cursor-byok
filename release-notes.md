## v0.0.51

### 新功能
- **中转站余额查询**：支持查询 OpenAI billing、NewAPI/OneAPI 等中转站余额、已用额度与剩余额度；模型配置供应商卡片支持懒查询，供应商详情页顶部支持查询与刷新，不支持时降级提示。
- **按中转站消耗统计**：首页新增「站点消耗」卡片，一眼可见各中转站历史花费与合计；会话分析页新增站点消耗明细表。
- **计费准确性**：基于模型配置价格（`/models` 返回或手填）逐请求计算美元花费，写入用量记录并在请求明细、首页价值估算统一展示；未配置价格时不臆造。
- **本地响应缓存**：可对完全相同的请求复用上次响应以减少 token 支出，默认关闭、仅精确匹配命中、不影响 agent 正确性；本地配置页可开关并设置 TTL/最大条数；首页新增「本地缓存命中」统计，与 provider prompt 缓存分开展示。

### 修复
- **修复编辑模型渲染崩溃**：ModelEditor 编辑已存在模型时因 `providerIcon` 未导入导致的渲染崩溃。
- **修复请求明细翻页被打回**：5 秒自动刷新不再把分页重置回第 1 页。
- **修复分组跳转失效**：模型配置页现能识别「模型分组」传来的分组聚焦。
- **修复缓存命中率开关无效**：`IncludeCacheWriteInHitRate` 配置现被前后端真正消费，口径一致。
- **批量删除原子化**：供应商批量删除改为单次保存，避免中途失败留下半删状态。
- **批量测试可真正取消**：取消不再仅是 UI 标记，会短路 worker 队列。
- **修复缓存开关被清空**：`localResponseCache` 纳入配置保存白名单，任意一次保存配置不再把它重置为默认。

### 优化
- **自研 Agent 调用稳定性**：瞬时错误（网络/429/5xx）自动重试 + 指数退避 + Retry-After；单通道下永久错误不再无意义重试，多通道自动轮换；OpenAI 工具调用 JSON 在适配器内提前校验，畸形参数清晰归因；assistant 记录解析失败不再静默丢弃。
- **模型探测更健壮**：探活改为结构化状态码判定，成功要求真实内容事件，避免假阳性。
- **首页价值估算统一口径**：移除硬编码价表，统一读取后端成本，并标注未计价请求数。
- **元数据缓存**：模型列表与余额查询结果本地 TTL 缓存，减少重复网络往返。
- 清理死代码、统一文案与 Opus 版本标签等若干打磨。

> **Windows 用户注意**：安装时若被 SmartScreen 拦截，点击「更多信息」->「仍要运行」即可。
