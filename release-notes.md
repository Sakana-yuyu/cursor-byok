## v0.0.61

### 修复
- **中转站 max_tokens 超限自动恢复**：识别 provider 返回的真实 max_tokens 限制，自动降低请求上限并重试，避免因静态模型上限过高导致整轮失败。
- **max_tokens 限制按渠道持久化**：将中转站反馈的限制精确保存到产生错误的渠道配置，不影响同一模型的其他渠道。
- **流式请求中断提示优化**：保留渠道错误身份，便于定位具体 provider 和中转站问题。

### 优化
- **流式空闲超时调整**：默认 provider 流式空闲 watchdog 从 240 秒缩短至 90 秒，更快结束长时间无响应的连接。

> **Windows 用户注意**：安装时若被 SmartScreen 拦截，点击「更多信息」->「仍要运行」即可。
