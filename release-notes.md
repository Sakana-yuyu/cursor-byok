## v0.0.60

### 修复
- **统计浮窗关闭主界面后卡死（重大）**：开启浮窗后关闭主界面，点击/拖动浮窗会导致整个界面无法点击，只能 Alt-Tab 切换窗口才能恢复。根因是浮窗被设为 `WS_EX_NOACTIVATE`（永不激活、无法成为前台窗口），主窗口隐藏后线程失去前台、浮窗无法接管，导致拖拽 capture 握手失败、运行时拖拽状态卡死并吞掉所有点击。现改用 `WS_EX_TOOLWINDOW` 实现任务栏隐藏但允许激活，主窗口隐藏后浮窗仍可正常拖拽与点击。
- **首页「对话轮次」恒为 0**：因后端过滤掉 `turn_finalized` 事件（修「两条明细」bug 的副作用），首页轮次统计失去数据源且无兜底，恒显示 0。现按时间范围从 `turn_finalized` 事件独立聚合轮次（与 token 分离、不重复计入），并给 `RangeSummary` 增加 turns 字段、前端加 `rangeSummary` 兜底，轮次统计恢复正常。
- **「请求明细」异常计数只统计当前页**：分页后「异常 N 条」只反映当前页而非全量，误导用户。现由后端按全量 `provider_call` 事件统计异常数（口径与前端一致：provider_error/no_usage 或无 usage 且 token 全 0），前端改读后端全量值。

### 优化
- **卡片式浮窗显示被遮挡**：卡片式浮窗竖向空间过紧，刷新动效的 text-shadow 与边框导致内容被 `overflow:hidden` 裁切。适度抬高窗口高度（132->144px）、收紧内部间距、收小 text-shadow，刷新时不再被遮挡。
- **球形浮窗重做**：原球形仅为 44px 2D 平面圆 + 4 个静态深色实心块，缺动效。重做为带 3D 立体感的核心球体（多层径向渐变 + 高光 + 外辉光 + 旋转环），四周数据卡透明化（玻璃质感 + 背景模糊），并加入悬浮/脉动/原位漂浮等动效（文字不旋转保持可读）。

> **Windows 用户注意**：安装时若被 SmartScreen 拦截，点击「更多信息」->「仍要运行」即可。
