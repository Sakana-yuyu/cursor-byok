## v0.0.45

### 新功能
- **会话分析详情页**：首页统计卡片右上角新增「详情」按钮，点击进入 `/metrics-detail` 页面。
  - 支持时间范围筛选：当日 / 近24小时 / 近3天 / 近7天 / 近30天 / 自定义日期范围。
  - 支持按模型筛选，查看单个模型的 token 消耗趋势。
  - ECharts 折线图按小时/天分桶展示：输入、输出、缓存读取、缓存写入、总消耗、缓存率（%）。
  - 汇总数字卡片实时联动筛选范围。
  - 支持缩放（dataZoom）查看局部细节。

> **Windows 用户注意**：安装时若被 SmartScreen 拦截，点击「更多信息」→「仍要运行」即可。这是 Windows 对未购买代码签名证书的应用的标准行为，程序本身安全。