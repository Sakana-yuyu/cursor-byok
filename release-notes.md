## v0.0.57

### 修复
- **浏览器预览白屏**：补齐 `AutoMatchContextWindows` 浏览器 mock 绑定，避免 `clientApi.js` 因缺少模块导出而启动失败。
- **子代理期间请求互相打断**：子代理运行期间的新请求进入会话队列，当前回合结束后自动继续处理，避免 stream 被提前替换。
- **工具回合 stale 误判**：修正 append 序列超时处理，避免客户端未重置序号时被错误重置并导致工具结果永久等待。

### 新功能
- **自动配对模型上下文窗口**：启动时、配置页和前端按钮均可触发模型上下文窗口自动配对；优先使用内置目录，未命中时探测供应商模型目录。
- **内置开发技能**：加入 `find-skills` 与 Superpowers 技能资源，并在非 Debug agent 请求中注入全局技能提示。

### 优化
- **供应商卡片展示**：外层模型卡片统一显示模型名称、模型 ID 和供应商图标；多模型连接会显示模型数量，分组名与接口地址仍保留。
- **浏览器预览状态**：补齐自动配对的浏览器预览返回值，保持桌面端与预览端调用契约一致。

> **Windows 用户注意**：安装时若被 SmartScreen 拦截，点击「更多信息」->「仍要运行」即可。