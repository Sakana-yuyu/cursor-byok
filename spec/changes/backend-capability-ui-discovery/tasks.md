# Protocol-fidelity capture tasks

- [x] 1. 保真记录数据模型与隔离开关
  - [x] 1.1 为记录器增加仅隔离模式启用的 body 编码、长度、摘要和 Base64 表示。
  - [x] 1.2 保持普通镜像记录字段和直通语义不变。
  - [x] 1.3 运行既有 MITM 定向测试、构建和静态检查。

- [ ] 2. Bidi protobuf 摘要与关联
  - [ ] 2.1 解码外层 `BidiAppendRequest`，并复用现有 Agent 消息解码器生成最小安全摘要。
  - [ ] 2.2 为摘要和保真记录增加 request ID 哈希、append 序号与显式解析失败标记。
  - [ ] 2.3 核验未知或畸形输入不阻断官方直通。

- [ ] 3. RunSSE 协议帧解析
  - [ ] 3.1 在响应 tee 中按 Connect/SSE 帧边界缓冲并保真记录，而非按底层读取分块记录。
  - [ ] 3.2 尽力解析 Agent 服务端消息类型并保留无法解析的帧。
  - [ ] 3.3 核验截断、断流和多个帧共用读取缓冲的边界。

- [ ] 4. Multitask 时间线索引
  - [ ] 4.1 以 `requestIdHash`、`exchangeId`、方向与局部序号写入隔离协议索引。
  - [ ] 4.2 为 Multitask、子代理状态与终态事件生成安全摘要。
  - [ ] 4.3 用用户控制的隔离 Multitask 请求做无正文结构化验收。

- [ ] 5. 台账与独立提交
  - [ ] 5.1 更新验证台账，区分构建、字节保真、协议解析和真实 Cursor E2E。
  - [ ] 5.2 每个已验证节点单独提交，不混入用户未跟踪文件。
